---
title: "Company Search"
description: "Find Facebook Ad Library companies and page IDs."
openapi: "/api-manual/agent-api/agent-api.json POST /v1/facebook-ad-library/company-search"
---

# Company Search

Search for public companies and retrieve the page ID required by Company Ads.

## Request Example

```json
{
  "query": "Nike"
}
```

{/* mcp-usage:start */}
## Use with MCP

| Interface | Value |
| --- | --- |
| REST | `POST /v1/facebook-ad-library/company-search` |
| Compact MCP | `socq_execute` with `endpoint: "facebook-ad-library/company-search"` |
| Typed MCP | `socq_facebook_ad_library_company_search` |
| Exact-tool URL | `https://api.socq.ai/mcp?tools=facebook_ad_library_company_search` |
| CLI | `socq facebook-ad-library company-search` |

Use the JSON request body above as the nested `input` object for `socq_execute`, or pass the same fields directly to the typed tool. Typed tools also accept `_wait_seconds`, `_result_limit`, and `_idempotency_key`.

<Info>
  A successful collection returns a `task_id`. Continue with `socq_get_task` until the task succeeds, then read normalized records from `results.items`.
</Info>

See [MCP quickstart](/integrations/mcp) for the complete submission and polling flow.
{/* mcp-usage:end */}

This endpoint saves every company candidate returned by the search request.






{/* result-schema:start */}
## Standard Result Fields

Poll `GET /v1/tasks/{task_id}?view=standard` for the fixed result contract. For MCP use `view`/`fields` on `socq_get_task` or `_result_view`/`_result_fields` on typed tools; CLI uses `--result-view`/`--result-fields`. Declared fields remain present when their value is `null`; use the task files for complete source data.

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `id` | `string` | Yes | Normalized id field. |
| `platform` | `string` | No | Normalized platform field. |
| `resource` | `string` | No | Normalized resource field. |
| `type` | `string` | No | Normalized type field. |
| `url` | `string` | Yes | Normalized url field. |
| `published_at` | `string` | Yes | Normalized published at field. |
| `collected_at` | `string` | No | Normalized collected at field. |
| `name` | `string` | Yes | Normalized name field. |
| `website` | `string` | Yes | Normalized website field. |
| `logo_url` | `string` | Yes | Normalized logo url field. |
| `region` | `string` | Yes | Normalized region field. |
| `ads_count` | `integer` | Yes | Normalized ads count field. |
{/* result-schema:end */}

## Completed Task Response

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "facebook-ad-library/company-search",
    "result_count": 1,
    "credits_amount": 0.5,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "view": "standard",
      "schema_id": "advertiser",
      "schema_version": "1.0",
      "items": [
        {
          "id": "51212153078",
          "platform": "facebook-ad-library",
          "resource": "company-search",
          "type": "company",
          "url": "https://www.facebook.com/example",
          "published_at": null,
          "collected_at": "2026-07-18T10:30:00Z",
          "name": "Example Sports",
          "website": null,
          "logo_url": null,
          "region": null,
          "ads_count": null
        }
      ]
    }
  }
}
```
