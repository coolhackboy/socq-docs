---
title: "TikTok Shop Products"
description: "Collect products from one public TikTok Shop."
openapi: "/api-manual/agent-api/agent-api.json POST /v1/tiktok-shop/products"
---

# TikTok Shop Products

Collect products from one store URL, sorted by best sellers or newest releases.

## Request Example

```json
{
  "url": "https://www.tiktok.com/shop/store/cmf-by-nothing/7495669706651568260",
  "results_limit": 20
}
```

{/* mcp-usage:start */}
## Use with MCP

| Interface | Value |
| --- | --- |
| REST | `POST /v1/tiktok-shop/products` |
| Compact MCP | `socq_execute` with `endpoint: "tiktok-shop/products"` |
| Typed MCP | `socq_tiktok_shop_products` |
| Exact-tool URL | `https://api.socq.ai/mcp?tools=tiktok_shop_products` |
| CLI | `socq tiktok-shop products` |

Use the JSON request body above as the nested `input` object for `socq_execute`, or pass the same fields directly to the typed tool. Typed tools also accept `_wait_seconds`, `_result_limit`, and `_idempotency_key`.

<Info>
  A successful collection returns a `task_id`. Continue with `socq_get_task` until the task succeeds, then read normalized records from `results.items`.
</Info>

See [MCP quickstart](/integrations/mcp) for the complete submission and polling flow.
{/* mcp-usage:end */}

One task accepts one store URL. The final result can contain fewer records than `results_limit` when no additional page is available.






{/* result-schema:start */}
## Standard Result Fields

Poll `GET /v1/tasks/{task_id}?view=standard` for the fixed result contract. For MCP use `view`/`fields` on `socq_get_task` or `_result_view`/`_result_fields` on typed tools; CLI uses `--result-view`/`--result-fields`. Declared fields remain present when their value is `null`; use the task files for complete source data.

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `id` | `string` | Yes | Normalized id field. |
| `platform` | `string` | No | Normalized platform field. |
| `resource` | `string` | No | Normalized resource field. |
| `type` | `string` | No | Normalized type field. |
| `url` | `string` | Yes | Normalized url field. |
| `published_at` | `string` | Yes | Normalized published at field. |
| `collected_at` | `string` | No | Normalized collected at field. |
| `title` | `string` | Yes | Normalized title field. |
| `text` | `string` | Yes | Normalized text field. |
| `seller` | `object` | Yes | Normalized seller field. |
| `seller.id` | `string` | Yes | Normalized id field. |
| `seller.username` | `string` | Yes | Normalized username field. |
| `seller.name` | `string` | Yes | Normalized name field. |
| `seller.url` | `string` | Yes | Normalized url field. |
| `seller.avatar_url` | `string` | Yes | Normalized avatar url field. |
| `seller.is_verified` | `boolean` | Yes | Normalized is verified field. |
| `seller.is_private` | `boolean` | Yes | Normalized is private field. |
| `price` | `object` | Yes | Normalized price field. |
| `price.amount` | `number` | Yes | Normalized amount field. |
| `price.currency` | `string` | Yes | Normalized currency field. |
| `price.formatted` | `string` | Yes | Normalized formatted field. |
| `rating` | `number` | Yes | Normalized rating field. |
| `reviews_count` | `integer` | Yes | Normalized reviews count field. |
| `sold_count` | `integer` | Yes | Normalized sold count field. |
| `media` | `array<object>` | Yes | Normalized media field. |
| `media.type` | `string` | Yes | Normalized type field. |
| `media.url` | `string` | Yes | Normalized url field. |
| `media.thumbnail_url` | `string` | Yes | Normalized thumbnail url field. |
| `media.width` | `integer` | Yes | Normalized width field. |
| `media.height` | `integer` | Yes | Normalized height field. |
| `media.duration_seconds` | `number` | Yes | Normalized duration seconds field. |
{/* result-schema:end */}

## Completed Task Response

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "tiktok-shop/products",
    "result_count": 1,
    "credits_amount": 0.7,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "view": "standard",
      "schema_id": "product",
      "schema_version": "1.0",
      "items": [
        {
          "id": "1729527313880355335",
          "platform": "tiktok-shop",
          "resource": "products",
          "type": "product",
          "url": "https://www.tiktok.com/shop/pdp/example/1729527313880355335",
          "published_at": null,
          "collected_at": "2026-07-18T10:30:00Z",
          "title": "Wellness Gummies",
          "text": "Wellness Gummies",
          "seller": null,
          "price": null,
          "rating": null,
          "reviews_count": null,
          "sold_count": null,
          "media": [
            {
              "type": "image",
              "url": "https://example.com/gummies.jpg",
              "thumbnail_url": null,
              "width": null,
              "height": null,
              "duration_seconds": null
            }
          ]
        }
      ]
    }
  }
}
```
