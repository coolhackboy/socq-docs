---
title: "Keyword Search Volume"
description: "Batch search volume, CPC, competition, and monthly trends."
openapi: "/api-manual/agent-api/agent-api.json POST /v1/seo/keyword-search-volume"
---

# Keyword Search Volume

```json
{"keywords":["keyword research","seo tools"],"location_code":2840,"language_code":"en"}
```

Accepts up to 1,000 keywords. Each returned record corresponds to a keyword for which metrics are available. Similar keywords can occasionally share aggregated search-volume values.








{/* result-schema:start */}
## Result Fields

Task results use the stable SocQ fields below. Source-specific field names and formats are normalized without inferring or enriching values that the source did not provide. To request a smaller field set, use `fields` for MCP queries, `_result_fields` for typed tools, or `--result-fields` in the CLI. Declared fields remain present when their value is `null`.

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `id` | `string` | Yes | Unique identifier for this result item. |
| `platform` | `string` | No | Platform the result belongs to, such as `instagram` or `linkedin`. |
| `resource` | `string` | No | SocQ resource represented by the result, such as `videos` or `comments`. |
| `type` | `string` | No | Category of the result item, such as `video`, `post`, or `comment`. |
| `url` | `string` | Yes | Public URL of the result item when available. |
| `published_at` | `string` | Yes | Time the source item was published, in ISO 8601 format when available. |
| `collected_at` | `string` | No | Time SocQ collected or processed this result, in ISO 8601 format. |
| `keyword` | `string` | Yes | Keyword represented by the result. |
| `search_volume` | `integer` | Yes | Estimated number of searches for the keyword in the configured period and market. |
| `difficulty` | `number` | Yes | Estimated ranking difficulty score for the keyword. |
| `intent` | `string` | Yes | Detected primary search intent for the keyword. |
| `cpc` | `number` | Yes | Estimated cost per click for the keyword. |
| `competition` | `number` | Yes | Advertiser competition level for the keyword. |
| `rank` | `integer` | Yes | Ranking position of the target page for the keyword. |
| `target_url` | `string` | Yes | Page URL associated with the keyword ranking. |
{/* result-schema:end */}

## Completed Task Response

```json
{
  "code": 200,
  "data": {
    "task_id": "C7P8NOJPFV7838GU",
    "status": "succeeded",
    "public_id": "seo/keyword-search-volume",
    "result_count": 1,
    "credits_amount": 24,
    "results": {
      "limit": 1,
      "next_cursor": null,
      "has_more": false,
      "schema_id": "seo-keyword",
      "schema_version": "1.0",
      "items": [
        {
          "id": "keyword research",
          "platform": "seo",
          "resource": "keyword-search-volume",
          "type": "keyword-search-volume",
          "url": null,
          "published_at": null,
          "collected_at": "2026-07-31T02:25:10.519540Z",
          "keyword": "keyword research",
          "search_volume": null,
          "difficulty": null,
          "intent": null,
          "cpc": null,
          "competition": null,
          "rank": null,
          "target_url": null
        }
      ]
    }
  }
}
```
