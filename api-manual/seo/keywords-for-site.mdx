---
title: "Keywords for Site"
description: "Generate keyword ideas from a website topic."
openapi: "/api-manual/agent-api/agent-api.json POST /v1/seo/keywords-for-site"
---

# Keywords for Site

```json
{
  "target": "microsoft.com",
  "location_code": 2840,
  "language_code": "en",
  "results_limit": 5
}
```

Each item is one website-relevant keyword record with available volume, competition, CPC, and trend metrics.








{/* result-schema:start */}
## Standard Result Fields

Poll `GET /v1/tasks/{task_id}?view=standard` for the fixed result contract. The standard view maps source-specific field names and formats to the stable SocQ fields below; it does not infer or enrich values that the source did not provide. For MCP use `view`/`fields` on `socq_get_task` or `_result_view`/`_result_fields` on typed tools; CLI uses `--result-view`/`--result-fields`. Declared fields remain present when their value is `null`; use the task files for complete source data.

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `id` | `string` | Yes | Unique identifier for this result item. |
| `platform` | `string` | No | Platform the result belongs to, such as `instagram` or `linkedin`. |
| `resource` | `string` | No | SocQ resource represented by the result, such as `videos` or `comments`. |
| `type` | `string` | No | Category of the result item, such as `video`, `post`, or `comment`. |
| `url` | `string` | Yes | Public URL of the result item when available. |
| `published_at` | `string` | Yes | Time the source item was published, in ISO 8601 format when available. |
| `collected_at` | `string` | No | Time SocQ collected or processed this result, in ISO 8601 format. |
| `keyword` | `string` | Yes | Keyword represented by the result. |
| `search_volume` | `integer` | Yes | Estimated number of searches for the keyword in the configured period and market. |
| `difficulty` | `number` | Yes | Estimated ranking difficulty score for the keyword. |
| `intent` | `string` | Yes | Detected primary search intent for the keyword. |
| `cpc` | `number` | Yes | Estimated cost per click for the keyword. |
| `competition` | `number` | Yes | Advertiser competition level for the keyword. |
| `rank` | `integer` | Yes | Ranking position of the target page for the keyword. |
| `target_url` | `string` | Yes | Page URL associated with the keyword ranking. |
{/* result-schema:end */}

## Completed Task Response

```json
{
  "code": 200,
  "data": {
    "task_id": "PBBP1MF42C6LTVVN",
    "status": "succeeded",
    "public_id": "seo/keywords-for-site",
    "result_count": 5,
    "credits_amount": 0.5,
    "results": {
      "limit": 1,
      "next_cursor": "OTc4Mw",
      "has_more": true,
      "view": "standard",
      "schema_id": "seo-keyword",
      "schema_version": "1.0",
      "items": [
        {
          "id": "azure and microsoft teams",
          "platform": "seo",
          "resource": "keywords-for-site",
          "type": "keywords-for-site",
          "url": null,
          "published_at": null,
          "collected_at": "2026-07-31T02:44:35.783531Z",
          "keyword": "azure and microsoft teams",
          "search_volume": null,
          "difficulty": null,
          "intent": "{'se_type': 'google', 'main_intent': 'navigational', 'foreign_intent': None, 'last_updated_time': '2026-04-29 00:59:12 +00:00'}",
          "cpc": null,
          "competition": null,
          "rank": null,
          "target_url": null
        }
      ]
    }
  }
}
```
