---
title: "Facebook Profile Events"
description: "Collect public events associated with Facebook profiles."
openapi: "/api-manual/agent-api/agent-api.json POST /v1/facebook/profile-events"
---

# Facebook Profile Events

Collect public events associated with Facebook profiles. Results are normalized and billed by the actual saved record count.

```json
{
  "urls": [
    "https://www.facebook.com/NASA"
  ],
  "results_limit": 20
}
```

{/* mcp-usage:start */}
## Use with MCP

| Interface | Value |
| --- | --- |
| REST | `POST /v1/facebook/profile-events` |
| Compact MCP | `socq_execute` with `endpoint: "facebook/profile-events"` |
| Typed MCP | `socq_facebook_profile_events` |
| Exact-tool URL | `https://api.socq.ai/mcp?tools=facebook_profile_events` |
| CLI | `socq facebook profile-events` |

Use the JSON request body above as the nested `input` object for `socq_execute`, or pass the same fields directly to the typed tool. Typed tools also accept `_wait_seconds`, `_result_limit`, and `_idempotency_key`.

<Info>
  A successful submission returns a `task_id`. Continue with `socq_get_task` until the task succeeds, then read normalized records from `results.items`.
</Info>

See [MCP quickstart](/integrations/mcp) for the complete submission and polling flow.
{/* mcp-usage:end */}






{/* result-schema:start */}
## Standard Result Fields

Poll `GET /v1/tasks/{task_id}?view=standard` for the fixed result contract. For MCP use `view`/`fields` on `socq_get_task` or `_result_view`/`_result_fields` on typed tools; CLI uses `--result-view`/`--result-fields`. Declared fields remain present when their value is `null`; use the task files for complete source data.

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `id` | `string` | Yes | Normalized id field. |
| `platform` | `string` | No | Normalized platform field. |
| `resource` | `string` | No | Normalized resource field. |
| `type` | `string` | No | Normalized type field. |
| `url` | `string` | Yes | Normalized url field. |
| `published_at` | `string` | Yes | Normalized published at field. |
| `collected_at` | `string` | No | Normalized collected at field. |
| `title` | `string` | Yes | Normalized title field. |
| `text` | `string` | Yes | Normalized text field. |
| `organizer` | `object` | Yes | Normalized organizer field. |
| `organizer.id` | `string` | Yes | Normalized id field. |
| `organizer.username` | `string` | Yes | Normalized username field. |
| `organizer.name` | `string` | Yes | Normalized name field. |
| `organizer.url` | `string` | Yes | Normalized url field. |
| `organizer.avatar_url` | `string` | Yes | Normalized avatar url field. |
| `organizer.is_verified` | `boolean` | Yes | Normalized is verified field. |
| `organizer.is_private` | `boolean` | Yes | Normalized is private field. |
| `location` | `object` | Yes | Normalized location field. |
| `location.name` | `string` | Yes | Normalized name field. |
| `location.city` | `string` | Yes | Normalized city field. |
| `location.region` | `string` | Yes | Normalized region field. |
| `location.country` | `string` | Yes | Normalized country field. |
| `location.latitude` | `number` | Yes | Normalized latitude field. |
| `location.longitude` | `number` | Yes | Normalized longitude field. |
| `start_at` | `string` | Yes | Normalized start at field. |
| `end_at` | `string` | Yes | Normalized end at field. |
| `media` | `array<object>` | Yes | Normalized media field. |
| `media.type` | `string` | Yes | Normalized type field. |
| `media.url` | `string` | Yes | Normalized url field. |
| `media.thumbnail_url` | `string` | Yes | Normalized thumbnail url field. |
| `media.width` | `integer` | Yes | Normalized width field. |
| `media.height` | `integer` | Yes | Normalized height field. |
| `media.duration_seconds` | `number` | Yes | Normalized duration seconds field. |
{/* result-schema:end */}

## Completed Task Response

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "facebook/profile-events",
    "result_count": 1,
    "credits_amount": 0.6,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "view": "standard",
      "schema_id": "event",
      "schema_version": "1.0",
      "items": [
        {
          "id": "example-id",
          "platform": "facebook",
          "resource": "profile-events",
          "type": "event",
          "url": "https://www.facebook.com/example/item",
          "published_at": "2026-07-24T08:00:00Z",
          "collected_at": "2026-07-24T10:30:00Z",
          "title": "Example public event content.",
          "text": "Example public event content.",
          "organizer": null,
          "location": null,
          "start_at": null,
          "end_at": null,
          "media": []
        }
      ]
    }
  }
}
```
