---
title: "Facebook Comment Replies"
description: "Collect public replies for a Facebook comment."
openapi: "/api-manual/agent-api/agent-api.json POST /v1/facebook/comment-replies"
---

# Facebook Comment Replies

Collect public replies for a Facebook comment. Results are normalized and billed by the actual saved record count.

```json
{
  "url": "https://www.facebook.com/NASA/posts/would-you-lie-in-bed-for-60-days-for-scientific-research11-volunteers-did-exactl/1271494461012594/",
  "comment_id": "Y29tbWVudDoxMjcxNDk0NDYxMDEyNTk0XzU4NzcxOTAzMTA0Mjk5MQ==",
  "results_limit": 20
}
```

<Info>
  Pass the stable post URL and parent comment ID. SOCQ finds the parent comment
  across comment pages and resolves the provider reply tokens internally. Legacy
  `feedback_id` plus `expansion_token` input remains supported, but those tokens
  can expire and should not be stored as a reusable request example.
</Info>

## Reply resolution errors

| Error code | Meaning |
| --- | --- |
| `facebook_parent_comment_not_found` | The comment was not found within the supported comment-page search range. Check that the URL and comment ID belong together. |
| `facebook_reply_token_missing` | The parent comment reports replies but does not expose the tokens required by the provider. |
| `facebook_reply_token_expired` | A supplied legacy token has expired. Retry with `url` and `comment_id`. |
| `facebook_reply_token_incompatible` | The provider returned a token that its reply endpoint would not accept. Retry later or use another parent comment. |
| `facebook_reply_upstream_failure` | The upstream Facebook reply service failed. Retry later. |

{/* mcp-usage:start */}
## Use with MCP

| Interface | Value |
| --- | --- |
| REST | `POST /v1/facebook/comment-replies` |
| Compact MCP | `socq_execute` with `endpoint: "facebook/comment-replies"` |
| Typed MCP | `socq_facebook_comment_replies` |
| Exact-tool URL | `https://api.socq.ai/mcp?tools=facebook_comment_replies` |
| CLI | `socq facebook comment-replies` |

Use the JSON request body above as the nested `input` object for `socq_execute`, or pass the same fields directly to the typed tool. Typed tools also accept `_wait_seconds`, `_result_limit`, and `_idempotency_key`.

<Info>
  A successful submission returns a `task_id`. Continue with `socq_get_task` until the task succeeds, then read normalized records from `results.items`.
</Info>

See [MCP quickstart](/integrations/mcp) for the complete submission and polling flow.
{/* mcp-usage:end */}








{/* result-schema:start */}
## Standard Result Fields

Poll `GET /v1/tasks/{task_id}?view=standard` for the fixed result contract. The standard view maps source-specific field names and formats to the stable SocQ fields below; it does not infer or enrich values that the source did not provide. For MCP use `view`/`fields` on `socq_get_task` or `_result_view`/`_result_fields` on typed tools; CLI uses `--result-view`/`--result-fields`. Declared fields remain present when their value is `null`; use the task files for complete source data.

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `id` | `string` | Yes | Unique identifier for this result item. |
| `platform` | `string` | No | Platform the result belongs to, such as `instagram` or `linkedin`. |
| `resource` | `string` | No | SocQ resource represented by the result, such as `videos` or `comments`. |
| `type` | `string` | No | Category of the result item, such as `video`, `post`, or `comment`. |
| `url` | `string` | Yes | Public URL of the result item when available. |
| `published_at` | `string` | Yes | Time the source item was published, in ISO 8601 format when available. |
| `collected_at` | `string` | No | Time SocQ collected or processed this result, in ISO 8601 format. |
| `text` | `string` | Yes | Comment or reply text. |
| `author` | `object` | Yes | Author or publisher of the content. |
| `author.id` | `string` | Yes | Unique identifier of the author. |
| `author.username` | `string` | Yes | Public username or handle of the account. |
| `author.name` | `string` | Yes | Display name of the author. |
| `author.url` | `string` | Yes | Public URL of the author when available. |
| `author.avatar_url` | `string` | Yes | URL of the account avatar image. |
| `author.is_verified` | `boolean` | Yes | Whether the account is verified on the platform. |
| `author.is_private` | `boolean` | Yes | Whether the account is private. |
| `parent_id` | `string` | Yes | Identifier of the parent comment for a reply. |
| `post_id` | `string` | Yes | Identifier of the post containing the comment. |
| `metrics` | `object` | Yes | Counters and measurements available for the result item. |
| `metrics.likes_count` | `integer` | Yes | Number of likes. |
| `metrics.replies_count` | `integer` | Yes | Number of replies. |
{/* result-schema:end */}

## Completed Task Response

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "facebook/comment-replies",
    "result_count": 1,
    "credits_amount": 0.3,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "view": "standard",
      "schema_id": "comment",
      "schema_version": "1.0",
      "items": [
        {
          "id": "example-id",
          "platform": "facebook",
          "resource": "comment-replies",
          "type": "comment",
          "url": "https://www.facebook.com/example/item",
          "published_at": "2026-07-24T08:00:00Z",
          "collected_at": "2026-07-24T10:30:00Z",
          "text": "Example public comment content.",
          "author": {
            "id": "author-id",
            "username": "example",
            "name": "Example",
            "url": "https://www.facebook.com/example",
            "avatar_url": null,
            "is_verified": null,
            "is_private": null
          },
          "parent_id": null,
          "post_id": null,
          "metrics": null
        }
      ]
    }
  }
}
```
