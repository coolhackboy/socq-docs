---
title: "Facebook Profiles"
description: "Collect public Facebook profile data."
openapi: "/api-manual/agent-api/agent-api.json POST /v1/facebook/profiles"
---

# Facebook Profiles

Collect public Facebook profile data. Results are normalized and billed by the actual saved record count.

```json
{
  "urls": [
    "https://www.facebook.com/NASA"
  ]
}
```

{/* mcp-usage:start */}
## Use with MCP

| Interface | Value |
| --- | --- |
| REST | `POST /v1/facebook/profiles` |
| Compact MCP | `socq_execute` with `endpoint: "facebook/profiles"` |
| Typed MCP | `socq_facebook_profiles` |
| Exact-tool URL | `https://api.socq.ai/mcp?tools=facebook_profiles` |
| CLI | `socq facebook profiles` |

Use the JSON request body above as the nested `input` object for `socq_execute`, or pass the same fields directly to the typed tool. Typed tools also accept `_wait_seconds`, `_result_limit`, and `_idempotency_key`.

<Info>
  A successful submission returns a `task_id`. Continue with `socq_get_task` until the task succeeds, then read normalized records from `results.items`.
</Info>

See [MCP quickstart](/integrations/mcp) for the complete submission and polling flow.
{/* mcp-usage:end */}

## Completed Task Response

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "facebook/profiles",
    "result_count": 1,
    "credits_amount": 1.2,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "items": [
        {
          "id": "example-id",
          "platform": "facebook",
          "resource": "profiles",
          "type": "profile",
          "url": "https://www.facebook.com/example",
          "text": null,
          "author": null,
          "metrics": {},
          "media": [],
          "created_at": "2026-07-24T08:00:00",
          "collected_at": "2026-07-24T10:30:00",
          "extra": {}
        }
      ]
    }
  }
}
```
