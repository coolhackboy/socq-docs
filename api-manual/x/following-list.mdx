---
title: "X Following List"
description: "Collect public X following profiles."
openapi: "/api-manual/agent-api/agent-api.json POST /v1/x/following-list"
---

# X Following List

Collect public X following profiles. Results are normalized and billed by the actual saved record count.

```json
{
  "usernames": [
    "@X"
  ],
  "results_limit": 200
}
```

{/* mcp-usage:start */}
## Use with MCP

| Interface | Value |
| --- | --- |
| REST | `POST /v1/x/following-list` |
| Compact MCP | `socq_execute` with `endpoint: "x/following-list"` |
| Typed MCP | `socq_x_following_list` |
| Exact-tool URL | `https://api.socq.ai/mcp?tools=x_following_list` |
| CLI | `socq x following-list` |

Use the JSON request body above as the nested `input` object for `socq_execute`, or pass the same fields directly to the typed tool. Typed tools also accept `_wait_seconds`, `_result_limit`, and `_idempotency_key`.

<Info>
  A successful submission returns a `task_id`. Continue with `socq_get_task` until the task succeeds, then read normalized records from `results.items`.
</Info>

See [MCP quickstart](/integrations/mcp) for the complete submission and polling flow.
{/* mcp-usage:end */}








{/* result-schema:start */}
## Standard Result Fields

Poll `GET /v1/tasks/{task_id}?view=standard` for the fixed result contract. The standard view maps source-specific field names and formats to the stable SocQ fields below; it does not infer or enrich values that the source did not provide. For MCP use `view`/`fields` on `socq_get_task` or `_result_view`/`_result_fields` on typed tools; CLI uses `--result-view`/`--result-fields`. Declared fields remain present when their value is `null`; use the task files for complete source data.

| Field | Type | Nullable | Description |
| --- | --- | --- | --- |
| `id` | `string` | Yes | Unique identifier for this result item. |
| `platform` | `string` | No | Platform the result belongs to, such as `instagram` or `linkedin`. |
| `resource` | `string` | No | SocQ resource represented by the result, such as `videos` or `comments`. |
| `type` | `string` | No | Category of the result item, such as `video`, `post`, or `comment`. |
| `url` | `string` | Yes | Public URL of the result item when available. |
| `published_at` | `string` | Yes | Time the source item was published, in ISO 8601 format when available. |
| `collected_at` | `string` | No | Time SocQ collected or processed this result, in ISO 8601 format. |
| `actor` | `object` | Yes | Account at the primary side of the relationship. |
| `actor.id` | `string` | Yes | Unique identifier of the primary account. |
| `actor.username` | `string` | Yes | Public username or handle of the account. |
| `actor.name` | `string` | Yes | Display name of the primary account. |
| `actor.url` | `string` | Yes | Public URL of the primary account when available. |
| `actor.avatar_url` | `string` | Yes | URL of the account avatar image. |
| `actor.is_verified` | `boolean` | Yes | Whether the account is verified on the platform. |
| `actor.is_private` | `boolean` | Yes | Whether the account is private. |
| `related_actor` | `object` | Yes | Account related to the primary account. |
| `related_actor.id` | `string` | Yes | Unique identifier of the related account. |
| `related_actor.username` | `string` | Yes | Public username or handle of the account. |
| `related_actor.name` | `string` | Yes | Display name of the related account. |
| `related_actor.url` | `string` | Yes | Public URL of the related account when available. |
| `related_actor.avatar_url` | `string` | Yes | URL of the account avatar image. |
| `related_actor.is_verified` | `boolean` | Yes | Whether the account is verified on the platform. |
| `related_actor.is_private` | `boolean` | Yes | Whether the account is private. |
| `relationship` | `string` | Yes | Relationship represented by the item, such as `follower`, `following`, or `retweeter`. |
{/* result-schema:end */}

## Completed Task Response

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "x/following-list",
    "result_count": 1,
    "credits_amount": 0.5,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "view": "standard",
      "schema_id": "relationship",
      "schema_version": "1.0",
      "items": [
        {
          "id": "example-id",
          "platform": "x",
          "resource": "following-list",
          "type": "profile",
          "url": "https://www.x.com/example",
          "published_at": "2026-07-24T08:00:00Z",
          "collected_at": "2026-07-24T10:30:00Z",
          "actor": null,
          "related_actor": {
            "id": "example-id",
            "username": null,
            "name": null,
            "url": "https://www.x.com/example",
            "avatar_url": null,
            "is_verified": null,
            "is_private": null
          },
          "relationship": "following"
        }
      ]
    }
  }
}
```
