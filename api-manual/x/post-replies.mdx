---
title: "X Post Replies"
description: "Collect public replies to X posts."
openapi: "/api-manual/agent-api/agent-api.json POST /v1/x/post-replies"
---

# X Post Replies

Collect public replies to X posts. Results are normalized and billed by the actual saved record count.

```json
{
  "urls": [
    "https://x.com/X/status/1234567890123456789"
  ],
  "results_limit": 20
}
```

{/* mcp-usage:start */}
## Use with MCP

| Interface | Value |
| --- | --- |
| REST | `POST /v1/x/post-replies` |
| Compact MCP | `socq_execute` with `endpoint: "x/post-replies"` |
| Typed MCP | `socq_x_post_replies` |
| Exact-tool URL | `https://api.socq.ai/mcp?tools=x_post_replies` |
| CLI | `socq x post-replies` |

Use the JSON request body above as the nested `input` object for `socq_execute`, or pass the same fields directly to the typed tool. Typed tools also accept `_wait_seconds`, `_result_limit`, and `_idempotency_key`.

<Info>
  A successful submission returns a `task_id`. Continue with `socq_get_task` until the task succeeds, then read normalized records from `results.items`.
</Info>

See [MCP quickstart](/integrations/mcp) for the complete submission and polling flow.
{/* mcp-usage:end */}

## Completed Task Response

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "x/post-replies",
    "result_count": 1,
    "credits_amount": 0.5,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "items": [
        {
          "id": "example-id",
          "platform": "x",
          "resource": "post-replies",
          "type": "comment",
          "url": "https://www.x.com/example/item",
          "text": "Example public comment content.",
          "author": {
            "id": "author-id",
            "username": "example",
            "name": "Example",
            "url": "https://www.x.com/example"
          },
          "metrics": {},
          "media": [],
          "created_at": "2026-07-24T08:00:00",
          "collected_at": "2026-07-24T10:30:00",
          "extra": {}
        }
      ]
    }
  }
}
```
