---
title: "Marketplace 商品详情"
description: "通过 URL 采集公共 Facebook Marketplace 商品详情。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/facebook-marketplace/item"
---

# Marketplace 商品详情

从一个或多个公共Marketplace 商品详情 URL 采集详细的列表信息。

每个 URL 都是独立处理的，最多可以生成一条商品信息。如果某个商品不可用或未找到，则剩余的 URL 将继续处理。

## 请求示例

```json
{
  "urls": [
    "https://www.facebook.com/marketplace/item/978385068564936/"
  ]
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/facebook-marketplace/item` |
| Compact MCP | `socq_execute` 与 `endpoint: "facebook-marketplace/item"` |
| Typed MCP | `socq_facebook_marketplace_item` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=facebook_marketplace_item` |
| CLI | `socq facebook-marketplace item` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}

根据成功返回的商品记录数进行计费。不产生记录的 URL 不包含在最终费用中。






{/* result-schema:start */}
## 标准结果字段

通过 `GET /v1/tasks/{task_id}?view=standard` 获取固定结果结构。MCP 查询使用 `view`/`fields`，Typed 工具使用 `_result_view`/`_result_fields`，CLI 使用 `--result-view`/`--result-fields`。声明字段在值为 `null` 时仍会保留；如需完整源数据，请使用任务结果文件。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 标准化的 id 字段。 |
| `platform` | `string` | 否 | 标准化的 platform 字段。 |
| `resource` | `string` | 否 | 标准化的 resource 字段。 |
| `type` | `string` | 否 | 标准化的 type 字段。 |
| `url` | `string` | 是 | 标准化的 url 字段。 |
| `published_at` | `string` | 是 | 标准化的 published at 字段。 |
| `collected_at` | `string` | 否 | 标准化的 collected at 字段。 |
| `title` | `string` | 是 | 标准化的 title 字段。 |
| `text` | `string` | 是 | 标准化的 text 字段。 |
| `seller` | `object` | 是 | 标准化的 seller 字段。 |
| `seller.id` | `string` | 是 | 标准化的 id 字段。 |
| `seller.username` | `string` | 是 | 标准化的 username 字段。 |
| `seller.name` | `string` | 是 | 标准化的 name 字段。 |
| `seller.url` | `string` | 是 | 标准化的 url 字段。 |
| `seller.avatar_url` | `string` | 是 | 标准化的 avatar url 字段。 |
| `seller.is_verified` | `boolean` | 是 | 标准化的 is verified 字段。 |
| `seller.is_private` | `boolean` | 是 | 标准化的 is private 字段。 |
| `price` | `object` | 是 | 标准化的 price 字段。 |
| `price.amount` | `number` | 是 | 标准化的 amount 字段。 |
| `price.currency` | `string` | 是 | 标准化的 currency 字段。 |
| `price.formatted` | `string` | 是 | 标准化的 formatted 字段。 |
| `location` | `object` | 是 | 标准化的 location 字段。 |
| `location.name` | `string` | 是 | 标准化的 name 字段。 |
| `location.city` | `string` | 是 | 标准化的 city 字段。 |
| `location.region` | `string` | 是 | 标准化的 region 字段。 |
| `location.country` | `string` | 是 | 标准化的 country 字段。 |
| `location.latitude` | `number` | 是 | 标准化的 latitude 字段。 |
| `location.longitude` | `number` | 是 | 标准化的 longitude 字段。 |
| `condition` | `string` | 是 | 标准化的 condition 字段。 |
| `availability` | `string` | 是 | 标准化的 availability 字段。 |
| `media` | `array<object>` | 是 | 标准化的 media 字段。 |
| `media.type` | `string` | 是 | 标准化的 type 字段。 |
| `media.url` | `string` | 是 | 标准化的 url 字段。 |
| `media.thumbnail_url` | `string` | 是 | 标准化的 thumbnail url 字段。 |
| `media.width` | `integer` | 是 | 标准化的 width 字段。 |
| `media.height` | `integer` | 是 | 标准化的 height 字段。 |
| `media.duration_seconds` | `number` | 是 | 标准化的 duration seconds 字段。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "facebook-marketplace/item",
    "result_count": 1,
    "credits_amount": 0.6,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "view": "standard",
      "schema_id": "listing",
      "schema_version": "1.0",
      "items": [
        {
          "id": "1656586118821988",
          "platform": "facebook-marketplace",
          "resource": "item",
          "type": "listing",
          "url": "https://www.facebook.com/marketplace/item/1656586118821988/",
          "published_at": "2026-05-15T16:57:39Z",
          "collected_at": "2026-07-18T10:30:00Z",
          "title": "Mountain bike",
          "text": "Used bike in good condition.",
          "seller": null,
          "price": null,
          "location": null,
          "condition": null,
          "availability": null,
          "media": [
            {
              "type": "image",
              "url": "https://example.com/item.jpg",
              "thumbnail_url": null,
              "width": 960,
              "height": 720,
              "duration_seconds": null
            }
          ]
        }
      ]
    }
  }
}
```
