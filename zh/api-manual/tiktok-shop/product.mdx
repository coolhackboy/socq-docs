---
title: "TikTok Shop 产品"
description: "采集一种公开 TikTok Shop 产品的详细信息。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/tiktok-shop/product"
---

# TikTok Shop 产品

采集产品详细信息、卖家数据、定价、类别、媒体和 SKU 信息。

## 请求示例

```json
{
  "url": "https://www.tiktok.com/shop/pdp/1729548220307968132"
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/tiktok-shop/product` |
| Compact MCP | `socq_execute` 与 `endpoint: "tiktok-shop/product"` |
| Typed MCP | `socq_tiktok_shop_product` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=tiktok_shop_product` |
| CLI | `socq tiktok-shop product` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}

一项任务接受一个产品 URL 并最多生成一条标准化产品记录。








{/* result-schema:start */}
## ????

????????????? SocQ ???????????????????????????????????????????????MCP ???? `fields`?Typed ???? `_result_fields`?CLI ?? `--result-fields`?????????? `null` ??????

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 当前结果项的唯一标识。 |
| `platform` | `string` | 否 | 结果所属的平台，例如 `instagram` 或 `linkedin`。 |
| `resource` | `string` | 否 | 该结果对应的 SocQ 资源，例如 `videos` 或 `comments`。 |
| `type` | `string` | 否 | 结果项的类别，例如 `video`、`post` 或 `comment`。 |
| `url` | `string` | 是 | 结果项的公开访问地址（如可获得）。 |
| `published_at` | `string` | 是 | 源内容的发布时间（如可获得），使用 ISO 8601 格式。 |
| `collected_at` | `string` | 否 | SocQ 采集或处理该结果的时间，使用 ISO 8601 格式。 |
| `title` | `string` | 是 | 商品标题。 |
| `text` | `string` | 是 | 商品描述。 |
| `seller` | `object` | 是 | 商品或条目关联的卖家。 |
| `seller.id` | `string` | 是 | 卖家的唯一标识。 |
| `seller.username` | `string` | 是 | 账号的公开用户名或账号标识。 |
| `seller.name` | `string` | 是 | 卖家的显示名称。 |
| `seller.url` | `string` | 是 | 卖家的公开访问地址（如可获得）。 |
| `seller.avatar_url` | `string` | 是 | 账号头像图片的地址。 |
| `seller.is_verified` | `boolean` | 是 | 账号是否已通过平台认证。 |
| `seller.is_private` | `boolean` | 是 | 账号是否为私密账号。 |
| `price` | `object` | 是 | 条目或商品的价格信息。 |
| `price.amount` | `number` | 是 | 货币金额的数值。 |
| `price.currency` | `string` | 是 | 货币金额所使用的币种代码或符号。 |
| `price.formatted` | `string` | 是 | 便于阅读的价格或薪酬文本。 |
| `rating` | `number` | 是 | 平台报告的商品平均评分。 |
| `reviews_count` | `integer` | 是 | 评价数量。 |
| `sold_count` | `integer` | 是 | 已售数量（如有报告）。 |
| `media` | `array<object>` | 是 | 结果附带的图片、视频或其他媒体。 |
| `media.type` | `string` | 是 | 媒体类型，例如 `image` 或 `video`。 |
| `media.url` | `string` | 是 | 媒体文件的地址。 |
| `media.thumbnail_url` | `string` | 是 | 媒体预览图的地址。 |
| `media.width` | `integer` | 是 | 媒体宽度，单位为像素。 |
| `media.height` | `integer` | 是 | 媒体高度，单位为像素。 |
| `media.duration_seconds` | `number` | 是 | 媒体时长，单位为秒。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "tiktok-shop/product",
    "result_count": 1,
    "credits_amount": 0.7,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "schema_id": "product",
      "schema_version": "1.0",
      "items": [
        {
          "id": "1729587769570529799",
          "platform": "tiktok-shop",
          "resource": "product",
          "type": "product",
          "url": "https://www.tiktok.com/shop/pdp/example-product/1729587769570529799",
          "published_at": null,
          "collected_at": "2026-07-18T10:30:00Z",
          "title": "Example Product",
          "text": "Example Product",
          "seller": null,
          "price": null,
          "rating": null,
          "reviews_count": null,
          "sold_count": null,
          "media": [
            {
              "type": "image",
              "url": "https://example.com/product.jpg",
              "thumbnail_url": null,
              "width": null,
              "height": null,
              "duration_seconds": null
            }
          ]
        }
      ]
    }
  }
}
```
