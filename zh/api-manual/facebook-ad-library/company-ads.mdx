---
title: "公司广告"
description: "采集 Facebook Ad Library 页面 ID 的公共广告。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/facebook-ad-library/company-ads"
---

# 公司广告

采集与公司搜索返回的一页 ID 关联的公共广告。

## 请求示例

```json
{
  "page_id": "51212153078",
  "results_limit": 100,
  "country": "US",
  "status": "ACTIVE",
  "media_type": "ALL"
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/facebook-ad-library/company-ads` |
| Compact MCP | `socq_execute` 与 `endpoint: "facebook-ad-library/company-ads"` |
| Typed MCP | `socq_facebook_ad_library_company_ads` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=facebook_ad_library_company_ads` |
| CLI | `socq facebook-ad-library company-ads` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}

当没有其他页面可用或重复的广告 ID 被删除时，最终结果包含的记录数可能少于 `results_limit`。






{/* result-schema:start */}
## 标准结果字段

通过 `GET /v1/tasks/{task_id}?view=standard` 获取固定结果结构。MCP 查询使用 `view`/`fields`，Typed 工具使用 `_result_view`/`_result_fields`，CLI 使用 `--result-view`/`--result-fields`。声明字段在值为 `null` 时仍会保留；如需完整源数据，请使用任务结果文件。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 标准化的 id 字段。 |
| `platform` | `string` | 否 | 标准化的 platform 字段。 |
| `resource` | `string` | 否 | 标准化的 resource 字段。 |
| `type` | `string` | 否 | 标准化的 type 字段。 |
| `url` | `string` | 是 | 标准化的 url 字段。 |
| `published_at` | `string` | 是 | 标准化的 published at 字段。 |
| `collected_at` | `string` | 否 | 标准化的 collected at 字段。 |
| `text` | `string` | 是 | 标准化的 text 字段。 |
| `advertiser` | `object` | 是 | 标准化的 advertiser 字段。 |
| `advertiser.id` | `string` | 是 | 标准化的 id 字段。 |
| `advertiser.name` | `string` | 是 | 标准化的 name 字段。 |
| `advertiser.url` | `string` | 是 | 标准化的 url 字段。 |
| `advertiser.logo_url` | `string` | 是 | 标准化的 logo url 字段。 |
| `start_date` | `string` | 是 | 标准化的 start date 字段。 |
| `end_date` | `string` | 是 | 标准化的 end date 字段。 |
| `countries` | `array<string>` | 是 | 标准化的 countries 字段。 |
| `platforms` | `array<string>` | 是 | 标准化的 platforms 字段。 |
| `format` | `string` | 是 | 标准化的 format 字段。 |
| `landing_page_url` | `string` | 是 | 标准化的 landing page url 字段。 |
| `impressions` | `integer` | 是 | 标准化的 impressions 字段。 |
| `media` | `array<object>` | 是 | 标准化的 media 字段。 |
| `media.type` | `string` | 是 | 标准化的 type 字段。 |
| `media.url` | `string` | 是 | 标准化的 url 字段。 |
| `media.thumbnail_url` | `string` | 是 | 标准化的 thumbnail url 字段。 |
| `media.width` | `integer` | 是 | 标准化的 width 字段。 |
| `media.height` | `integer` | 是 | 标准化的 height 字段。 |
| `media.duration_seconds` | `number` | 是 | 标准化的 duration seconds 字段。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "facebook-ad-library/company-ads",
    "result_count": 1,
    "credits_amount": 0.5,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "view": "standard",
      "schema_id": "ad",
      "schema_version": "1.0",
      "items": [
        {
          "id": "1162496978867592",
          "platform": "facebook-ad-library",
          "resource": "company-ads",
          "type": "ad",
          "url": "https://www.facebook.com/ads/library/?id=1162496978867592",
          "published_at": "2026-07-10T00:00:00Z",
          "collected_at": "2026-07-18T10:30:00Z",
          "text": "Explore the latest collection.",
          "advertiser": {
            "id": "51212153078",
            "name": "Example Sports",
            "url": null,
            "logo_url": null
          },
          "start_date": null,
          "end_date": null,
          "countries": null,
          "platforms": null,
          "format": null,
          "landing_page_url": null,
          "impressions": null,
          "media": [
            {
              "type": "image",
              "url": "https://example.com/ad.jpg",
              "thumbnail_url": null,
              "width": null,
              "height": null,
              "duration_seconds": null
            }
          ]
        }
      ]
    }
  }
}
```
