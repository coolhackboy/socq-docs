---
title: "公司搜索"
description: "查找 Facebook Ad Library 公司和页面 ID。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/facebook-ad-library/company-search"
---

# 公司搜索

搜索上市公司并检索公司广告所需的页面 ID。

## 请求示例

```json
{
  "query": "Nike"
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/facebook-ad-library/company-search` |
| Compact MCP | `socq_execute` 与 `endpoint: "facebook-ad-library/company-search"` |
| Typed MCP | `socq_facebook_ad_library_company_search` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=facebook_ad_library_company_search` |
| CLI | `socq facebook-ad-library company-search` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}

该端点保存搜索请求返回的每个公司候选者。








{/* result-schema:start */}
## 结果字段

任务结果使用下列稳定的 SocQ 字段。来源特有的字段名称和格式会被规范化，但不会推断或补充来源未提供的值。如需请求更小的字段集，MCP 查询请使用 `fields`，Typed 工具请使用 `_result_fields`，CLI 请使用 `--result-fields`。即使值为 `null`，已声明的字段仍会保留。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 当前结果项的唯一标识。 |
| `platform` | `string` | 否 | 结果所属的平台，例如 `instagram` 或 `linkedin`。 |
| `resource` | `string` | 否 | 该结果对应的 SocQ 资源，例如 `videos` 或 `comments`。 |
| `type` | `string` | 否 | 结果项的类别，例如 `video`、`post` 或 `comment`。 |
| `url` | `string` | 是 | 结果项的公开访问地址（如可获得）。 |
| `published_at` | `string` | 是 | 源内容的发布时间（如可获得），使用 ISO 8601 格式。 |
| `collected_at` | `string` | 否 | SocQ 采集或处理该结果的时间，使用 ISO 8601 格式。 |
| `name` | `string` | 是 | 广告主或机构名称。 |
| `website` | `string` | 是 | 账号、公司或广告主关联的网站。 |
| `logo_url` | `string` | 是 | 公司或广告主徽标的地址。 |
| `region` | `string` | 是 | 地区、州或省份名称。 |
| `ads_count` | `integer` | 是 | 与广告主关联的广告数量。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "facebook-ad-library/company-search",
    "result_count": 1,
    "credits_amount": 0.5,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "schema_id": "advertiser",
      "schema_version": "1.0",
      "items": [
        {
          "id": "51212153078",
          "platform": "facebook-ad-library",
          "resource": "company-search",
          "type": "company",
          "url": "https://www.facebook.com/example",
          "published_at": null,
          "collected_at": "2026-07-18T10:30:00Z",
          "name": "Example Sports",
          "website": null,
          "logo_url": null,
          "region": null,
          "ads_count": null
        }
      ]
    }
  }
}
```
