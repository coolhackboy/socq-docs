---
title: "LinkedIn 广告搜索"
description: "搜索并标准化公开的 LinkedIn 广告数据。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/linkedin-ad-library/search"
---

# LinkedIn 广告搜索

```json
{
  "company": "Microsoft",
  "countries": [
    "US"
  ],
  "results_limit": 24
}
```

`results_limit` 必须是每页 24 条的整数倍。








{/* result-schema:start */}
## 结果字段

任务结果使用下列稳定的 SocQ 字段。来源特有的字段名称和格式会被规范化，但不会推断或补充来源未提供的值。如需请求更小的字段集，MCP 查询请使用 `fields`，Typed 工具请使用 `_result_fields`，CLI 请使用 `--result-fields`。即使值为 `null`，已声明的字段仍会保留。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 当前结果项的唯一标识。 |
| `platform` | `string` | 否 | 结果所属的平台，例如 `instagram` 或 `linkedin`。 |
| `resource` | `string` | 否 | 该结果对应的 SocQ 资源，例如 `videos` 或 `comments`。 |
| `type` | `string` | 否 | 结果项的类别，例如 `video`、`post` 或 `comment`。 |
| `url` | `string` | 是 | 结果项的公开访问地址（如可获得）。 |
| `published_at` | `string` | 是 | 源内容的发布时间（如可获得），使用 ISO 8601 格式。 |
| `collected_at` | `string` | 否 | SocQ 采集或处理该结果的时间，使用 ISO 8601 格式。 |
| `text` | `string` | 是 | 广告文案或创意文本。 |
| `advertiser` | `object` | 是 | 广告的投放主体。 |
| `advertiser.id` | `string` | 是 | 广告主的唯一标识。 |
| `advertiser.name` | `string` | 是 | 广告主的显示名称。 |
| `advertiser.url` | `string` | 是 | 广告主的公开访问地址（如可获得）。 |
| `advertiser.logo_url` | `string` | 是 | 公司或广告主徽标的地址。 |
| `start_date` | `string` | 是 | 广告开始投放的日期（如可获得）。 |
| `end_date` | `string` | 是 | 广告结束投放的日期（如可获得）。 |
| `countries` | `array<string>` | 是 | 广告投放的国家或地区。 |
| `platforms` | `array<string>` | 是 | 广告投放的平台或版位。 |
| `format` | `string` | 是 | 广告创意格式，例如图片或视频。 |
| `landing_page_url` | `string` | 是 | 点击广告后打开的落地页地址。 |
| `impressions` | `integer` | 是 | 广告展示次数（如有报告）。 |
| `media` | `array<object>` | 是 | 结果附带的图片、视频或其他媒体。 |
| `media.type` | `string` | 是 | 媒体类型，例如 `image` 或 `video`。 |
| `media.url` | `string` | 是 | 媒体文件的地址。 |
| `media.thumbnail_url` | `string` | 是 | 媒体预览图的地址。 |
| `media.width` | `integer` | 是 | 媒体宽度，单位为像素。 |
| `media.height` | `integer` | 是 | 媒体高度，单位为像素。 |
| `media.duration_seconds` | `number` | 是 | 媒体时长，单位为秒。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "CMRCG314KXN6XW2D",
    "status": "succeeded",
    "public_id": "linkedin-ad-library/search",
    "result_count": 1,
    "credits_amount": 0.5,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "schema_id": "ad",
      "schema_version": "1.0",
      "items": [
        {
          "id": "1537832396",
          "platform": "linkedin-ad-library",
          "resource": "search",
          "type": "ad",
          "url": "https://www.linkedin.com/ad-library/detail/1537832396",
          "published_at": null,
          "collected_at": "2026-08-11T03:03:36.272665Z",
          "text": "Where leaders are winning with AI: everywhere.",
          "advertiser": {
            "id": null,
            "name": "Microsoft in Business",
            "url": "https://www.linkedin.com/company/107855954",
            "logo_url": null
          },
          "start_date": null,
          "end_date": null,
          "countries": null,
          "platforms": null,
          "format": null,
          "landing_page_url": null,
          "impressions": null,
          "media": [
            {
              "type": "video",
              "url": "https://dms.licdn.com/playlist/vid/v2/D4E10AQHHxfk-Y_8UCg/mp4-720p-30fp-crf28",
              "thumbnail_url": null,
              "width": null,
              "height": null,
              "duration_seconds": null
            }
          ]
        }
      ]
    }
  }
}
```

## 与 MCP 和 CLI 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/linkedin-ad-library/search` |
| Compact MCP | `socq_execute` 与 `endpoint: "linkedin-ad-library/search"` |
| Typed MCP | `socq_linkedin_ad_library_search` |
| CLI | `socq linkedin-ad-library search` |

该接口使用异步任务流程并返回 `task_id`。轮询任务接口以读取标准化结果。
