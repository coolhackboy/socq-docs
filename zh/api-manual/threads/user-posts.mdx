---
title: "Threads 用户帖子"
description: "从 Threads 个人资料中采集公共帖子。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/threads/user-posts"
---

# Threads 用户帖子

```json
{
  "urls": ["https://www.threads.com/@example"],
  "results_limit": 100
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/threads/user-posts` |
| Compact MCP | `socq_execute` 与 `endpoint: "threads/user-posts"` |
| Typed MCP | `socq_threads_user_posts` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=threads_user_posts` |
| CLI | `socq threads user-posts` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}

`results_limit` 默认为 100，接受 1 到 2,000 之间的值，并应用于每个个人资料 URL。结果使用 `resource="user-posts"` 和 `type="post"`。

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "threads/user-posts",
    "result_count": 1,
    "credits_amount": 0.5,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "items": [{
        "id": "POST_ID",
        "platform": "threads",
        "resource": "user-posts",
        "type": "post",
        "url": "https://www.threads.com/@example/post/POST_ID",
        "text": "Example public Threads post.",
        "author": {"id": "63319240287", "username": "example", "name": "Example Creator", "url": "https://www.threads.com/@example"},
        "metrics": {"likes_count": 1200, "comments_count": 48, "reposts_count": 32, "shares_count": 15},
        "media": [],
        "created_at": "2026-07-16T08:00:00",
        "collected_at": "2026-07-17T10:30:00",
        "extra": {"discovery_input": {"url": "https://www.threads.com/@example"}}
      }]
    }
  }
}
```
