---
title: "TikTok 个人资料"
description: "采集公共 TikTok 个人资料元数据和统计数据。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/tiktok/profiles"
---

# TikTok 简介

为每个公共用户名采集一个规范化的个人资料记录。领先的 `@` 是可选的。

```json
{
  "usernames": ["example", "@creator"]
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/tiktok/profiles` |
| Compact MCP | `socq_execute` 与 `endpoint: "tiktok/profiles"` |
| Typed MCP | `socq_tiktok_profiles` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=tiktok_profiles` |
| CLI | `socq tiktok profiles` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}

## 结果项

```json
{
  "id": "6784642169778881542",
  "platform": "tiktok",
  "resource": "profiles",
  "type": "profile",
  "username": "example",
  "name": "Example Creator",
  "url": "https://www.tiktok.com/@example",
  "description": "Public profile description",
  "avatar_url": "https://example.com/avatar.jpg",
  "is_verified": false,
  "is_private": false,
  "metrics": {
    "followers_count": 12500,
    "following_count": 120,
    "videos_count": 48,
    "likes_count": 98000
  },
  "created_at": null,
  "collected_at": "2026-07-15T10:30:00",
  "extra": {}
}
```

私人、不可用或空的个人资料可能不会产生结果记录。