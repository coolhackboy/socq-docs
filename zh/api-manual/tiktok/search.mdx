---
title: "TikTok 搜索"
description: "使用便携式过滤器搜索公共 TikTok 视频。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/tiktok/search"
---

# TikTok 搜索

使用稳定的过滤器搜索公共视频。

```json
{
  "query": "python tutorial",
  "results_limit": 50,
  "sort_by": "relevance",
  "published_within": "month"
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/tiktok/search` |
| Compact MCP | `socq_execute` 与 `endpoint: "tiktok/search"` |
| Typed MCP | `socq_tiktok_search` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=tiktok_search` |
| CLI | `socq tiktok search` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}

|领域|接受的值|
| --- | --- |
| `sort_by` | `relevance`、`date`、`likes` |
| `published_within` | `day`、`week`、`month`、`three_months`、`six_months` |

`results_limit` 默认为 100。

结果使用[TikTok 视频](/zh/api-manual/tiktok/videos) 中记录的归一化结构，其中 `resource` 设置为 `search`。如果可用，提交的查询将在 `extra.search_query` 下返回。








{/* result-schema:start */}
## 结果字段

任务结果使用下列稳定的 SocQ 字段。来源特有的字段名称和格式会被规范化，但不会推断或补充来源未提供的值。如需请求更小的字段集，MCP 查询请使用 `fields`，Typed 工具请使用 `_result_fields`，CLI 请使用 `--result-fields`。即使值为 `null`，已声明的字段仍会保留。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 当前结果项的唯一标识。 |
| `platform` | `string` | 否 | 结果所属的平台，例如 `instagram` 或 `linkedin`。 |
| `resource` | `string` | 否 | 该结果对应的 SocQ 资源，例如 `videos` 或 `comments`。 |
| `type` | `string` | 否 | 结果项的类别，例如 `video`、`post` 或 `comment`。 |
| `url` | `string` | 是 | 结果项的公开访问地址（如可获得）。 |
| `published_at` | `string` | 是 | 源内容的发布时间（如可获得），使用 ISO 8601 格式。 |
| `collected_at` | `string` | 否 | SocQ 采集或处理该结果的时间，使用 ISO 8601 格式。 |
| `text` | `string` | 是 | 视频的说明文字或描述。 |
| `author` | `object` | 是 | 内容的作者或发布者。 |
| `author.id` | `string` | 是 | 作者的唯一标识。 |
| `author.username` | `string` | 是 | 账号的公开用户名或账号标识。 |
| `author.name` | `string` | 是 | 作者的显示名称。 |
| `author.url` | `string` | 是 | 作者的公开访问地址（如可获得）。 |
| `author.avatar_url` | `string` | 是 | 账号头像图片的地址。 |
| `author.is_verified` | `boolean` | 是 | 账号是否已通过平台认证。 |
| `author.is_private` | `boolean` | 是 | 账号是否为私密账号。 |
| `metrics` | `object` | 是 | 结果项可获得的计数和度量数据。 |
| `metrics.likes_count` | `integer` | 是 | 点赞数量。 |
| `metrics.comments_count` | `integer` | 是 | 评论数量。 |
| `metrics.shares_count` | `integer` | 是 | 分享或转发数量。 |
| `metrics.plays_count` | `integer` | 是 | 平台报告的视频播放次数。 |
| `metrics.views_count` | `integer` | 是 | 平台报告的观看数量。 |
| `media` | `array<object>` | 是 | 结果附带的图片、视频或其他媒体。 |
| `media.type` | `string` | 是 | 媒体类型，例如 `image` 或 `video`。 |
| `media.url` | `string` | 是 | 媒体文件的地址。 |
| `media.thumbnail_url` | `string` | 是 | 媒体预览图的地址。 |
| `media.width` | `integer` | 是 | 媒体宽度，单位为像素。 |
| `media.height` | `integer` | 是 | 媒体高度，单位为像素。 |
| `media.duration_seconds` | `number` | 是 | 媒体时长，单位为秒。 |
| `hashtags` | `array<string>` | 是 | 内容关联的话题标签，保留源数据中的拼写。 |
| `mentions` | `array<string>` | 是 | 内容中提及的账号。 |
| `shortcode` | `string` | 是 | 平台用于标识媒体内容的短代码。 |
| `duration_seconds` | `number` | 是 | 时长，单位为秒。 |
| `audio` | `object` | 是 | 视频关联的音频或音乐信息。 |
| `audio.id` | `string` | 是 | 音频或音乐曲目的标识。 |
| `audio.title` | `string` | 是 | 音频或音乐曲目的标题。 |
| `audio.artist` | `string` | 是 | 音频艺术家或创作者名称。 |
| `audio.url` | `string` | 是 | 音频或音乐曲目的地址（如可获得）。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "5WC29DY8SSM6RZEH",
    "status": "succeeded",
    "public_id": "tiktok/search",
    "result_count": 1,
    "credits_amount": 0.7,
    "results": {
      "limit": 1,
      "next_cursor": null,
      "has_more": false,
      "schema_id": "reel-video",
      "schema_version": "1.0",
      "items": [
        {
          "id": "7662002084908190989",
          "platform": "tiktok",
          "resource": "search",
          "type": "video",
          "url": "https://www.tiktok.com/@yangzhujun1/video/7662002084908190989",
          "published_at": "2026-07-13T13:22:09Z",
          "collected_at": "2026-07-15T11:00:49.541712Z",
          "text": "Codex和GPT正式合并，内置最新发布的GPT5.6，你终于拥有了超能助理 #ai #GPT56 #Codex #ChatGPT #OpenAI",
          "author": {
            "id": "7407476938992174111",
            "username": "yangzhujun1",
            "name": "杨竹筠",
            "url": "https://www.tiktok.com/@yangzhujun1",
            "avatar_url": "https://p19-common-sign.tiktokcdn-us.com/tos-useast5-avt-0068-tx/911797ed4accd5f1d4990c82fa35b558~tplv-tiktokx-cropcenter:720:720.jpeg?dr=9640&refresh_token=93067c08&x-expires=1784286000&x-signature=oFgXWXLv2dPnatVaalcLbYgfaJw%3D&t=4d5b0474&ps=13740610&shp=a5d48078&shcp=81f88b70&idc=useast5",
            "is_verified": false,
            "is_private": false
          },
          "metrics": {
            "likes_count": 1315,
            "comments_count": 22,
            "shares_count": 316,
            "plays_count": null,
            "views_count": 52300
          },
          "media": [
            {
              "type": "image",
              "url": "https://p16-common-sign.tiktokcdn-us.com/tos-useast5-p-0068-tx/oArBFHpYAtAIRAnUqckLIzeqEEmetASUTAG5Ef~tplv-tiktokx-origin.image?dr=9636&x-expires=1784286000&x-signature=z7EjpJt%2FOe%2ByHByw6xNGzrI81pQ%3D&t=4d5b0474&ps=13740610&shp=81f88b70&shcp=43f4a2f9&idc=useast5",
              "thumbnail_url": null,
              "width": null,
              "height": null,
              "duration_seconds": null
            }
          ],
          "hashtags": [],
          "mentions": [],
          "shortcode": null,
          "duration_seconds": 128.0,
          "audio": null
        }
      ]
    }
  }
}
```
