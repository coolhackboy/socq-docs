---
title: "Facebook 公共主页"
description: "采集公共Facebook页面数据。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/facebook/pages"
---

# Facebook 公共主页

采集公共 Facebook 页面/个人资料元数据、公共 URL、个人资料字段和相关公共指标。

## 端点

```http
POST https://api.socq.ai/v1/facebook/pages
```

## 请求正文

```json
{
  "urls": ["https://www.facebook.com/facebook"]
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/facebook/pages` |
| Compact MCP | `socq_execute` 与 `endpoint: "facebook/pages"` |
| Typed MCP | `socq_facebook_pages` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=facebook_pages` |
| CLI | `socq facebook pages` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}








{/* result-schema:start */}
## 标准结果字段

通过 `GET /v1/tasks/{task_id}?view=standard` 获取固定结果结构。标准视图会将来源特有的字段名和格式映射为下表所列的稳定 SocQ 字段，但不会推断或补充来源未提供的数据。MCP 查询使用 `view`/`fields`，Typed 工具使用 `_result_view`/`_result_fields`，CLI 使用 `--result-view`/`--result-fields`。声明字段在值为 `null` 时仍会保留；如需完整源数据，请使用任务结果文件。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 当前结果项的唯一标识。 |
| `platform` | `string` | 否 | 结果所属的平台，例如 `instagram` 或 `linkedin`。 |
| `resource` | `string` | 否 | 该结果对应的 SocQ 资源，例如 `videos` 或 `comments`。 |
| `type` | `string` | 否 | 结果项的类别，例如 `video`、`post` 或 `comment`。 |
| `url` | `string` | 是 | 结果项的公开访问地址（如可获得）。 |
| `published_at` | `string` | 是 | 源内容的发布时间（如可获得），使用 ISO 8601 格式。 |
| `collected_at` | `string` | 否 | SocQ 采集或处理该结果的时间，使用 ISO 8601 格式。 |
| `username` | `string` | 是 | 账号的公开用户名或账号标识。 |
| `name` | `string` | 是 | 账号或主页的显示名称。 |
| `text` | `string` | 是 | 账号或主页的简介。 |
| `avatar_url` | `string` | 是 | 账号头像图片的地址。 |
| `cover_url` | `string` | 是 | 封面图片的地址。 |
| `website` | `string` | 是 | 账号、公司或广告主关联的网站。 |
| `is_verified` | `boolean` | 是 | 账号是否已通过平台认证。 |
| `is_private` | `boolean` | 是 | 账号是否为私密账号。 |
| `location` | `object` | 是 | 结果关联的地理位置信息。 |
| `location.name` | `string` | 是 | 便于阅读的地点名称。 |
| `location.city` | `string` | 是 | 城市名称。 |
| `location.region` | `string` | 是 | 地区、州或省份名称。 |
| `location.country` | `string` | 是 | 国家或地区名称或代码。 |
| `location.latitude` | `number` | 是 | 十进制度格式的纬度。 |
| `location.longitude` | `number` | 是 | 十进制度格式的经度。 |
| `metrics` | `object` | 是 | 结果项可获得的计数和度量数据。 |
| `metrics.followers_count` | `integer` | 是 | 粉丝数量。 |
| `metrics.following_count` | `integer` | 是 | 关注的账号数量。 |
| `metrics.posts_count` | `integer` | 是 | 帖子数量，或与趋势关联的帖子数量。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "facebook/pages",
    "result_count": 1,
    "credits_amount": 2.4,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "view": "standard",
      "schema_id": "account",
      "schema_version": "1.0",
      "items": [
        {
          "id": "100064860875397",
          "platform": "facebook",
          "resource": "pages",
          "type": "page",
          "url": "https://www.facebook.com/facebook/",
          "published_at": "2007-11-07T00:00:00Z",
          "collected_at": "2026-07-08T08:59:45Z",
          "username": null,
          "name": "Facebook",
          "text": null,
          "avatar_url": "https://example.com/avatar.jpg",
          "cover_url": "https://example.com/cover.jpg",
          "website": null,
          "is_verified": null,
          "is_private": null,
          "location": null,
          "metrics": {
            "followers_count": 154000000,
            "following_count": null,
            "posts_count": null
          }
        }
      ]
    }
  }
}
```

您可以在同一字段中传递多个页面 URL：

```json
{
  "urls": [
    "https://www.facebook.com/facebook",
    "https://www.facebook.com/Meta"
  ]
}
```
