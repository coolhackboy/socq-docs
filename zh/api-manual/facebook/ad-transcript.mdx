---
title: "Facebook 广告转录"
description: "采集公开 Facebook 广告的转录文本。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/facebook/ad-transcript"
---

# Facebook 广告转录

采集公开 Facebook 广告的转录文本。结果会转换为统一结构，并按实际保存的记录数计费。

```json
{
  "urls": [
    "https://www.facebook.com/ads/library?id=1545582153738441"
  ]
}
```

{/* mcp-usage:start */}
## 通过 MCP 使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/facebook/ad-transcript` |
| Compact MCP | `socq_execute`，并设置 `endpoint: "facebook/ad-transcript"` |
| 类型化 MCP | `socq_facebook_ad_transcript` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=facebook_ad_transcript` |
| CLI | `socq facebook ad-transcript` |

使用 `socq_execute` 时，将上面的 JSON 请求体作为 `input` 对象传入；也可以将相同字段直接传给类型化 MCP 工具。类型化 MCP 工具还支持 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  提交成功后会返回 `task_id`。继续调用 `socq_get_task` 直到任务成功，然后从 `results.items` 读取标准化记录。
</Info>

完整的提交和轮询流程请参阅 [MCP 快速开始](/zh/integrations/mcp)。
{/* mcp-usage:end */}








{/* result-schema:start */}
## ????

????????????? SocQ ???????????????????????????????????????????????MCP ???? `fields`?Typed ???? `_result_fields`?CLI ?? `--result-fields`?????????? `null` ??????

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 当前结果项的唯一标识。 |
| `platform` | `string` | 否 | 结果所属的平台，例如 `instagram` 或 `linkedin`。 |
| `resource` | `string` | 否 | 该结果对应的 SocQ 资源，例如 `videos` 或 `comments`。 |
| `type` | `string` | 否 | 结果项的类别，例如 `video`、`post` 或 `comment`。 |
| `url` | `string` | 是 | 结果项的公开访问地址（如可获得）。 |
| `published_at` | `string` | 是 | 源内容的发布时间（如可获得），使用 ISO 8601 格式。 |
| `collected_at` | `string` | 否 | SocQ 采集或处理该结果的时间，使用 ISO 8601 格式。 |
| `text` | `string` | 是 | 音频或视频内容的转录文本。 |
| `language` | `string` | 是 | 转录文本的语言（如可获得）。 |
| `format` | `string` | 是 | 转录结果的表示格式，例如纯文本或带时间片段。 |
| `duration_seconds` | `number` | 是 | 时长，单位为秒。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "facebook/ad-transcript",
    "result_count": 1,
    "credits_amount": 0.5,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "schema_id": "transcript",
      "schema_version": "1.0",
      "items": [
        {
          "id": "example-id",
          "platform": "facebook",
          "resource": "ad-transcript",
          "type": "transcript",
          "url": "https://www.facebook.com/example/item",
          "published_at": "2026-07-24T08:00:00Z",
          "collected_at": "2026-07-24T10:30:00Z",
          "text": "Example public transcript content.",
          "language": null,
          "format": null,
          "duration_seconds": null
        }
      ]
    }
  }
}
```
