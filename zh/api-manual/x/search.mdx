---
title: "X 搜索"
description: "搜索公共 X 帖子。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/x/search"
---

# X 搜索

使用关键字或搜索表达式搜索公共帖子。

```json
{
  "query": "AI lang:en",
  "results_limit": 100,
  "sort_by": "latest"
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/x/search` |
| Compact MCP | `socq_execute` 与 `endpoint: "x/search"` |
| Typed MCP | `socq_x_search` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=x_search` |
| CLI | `socq x search` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}

|领域|接受的值|
| --- | --- |
| `sort_by` | `latest`、`top` |

结果使用 [X 帖子](/zh/api-manual/x/posts) 中记录的归一化结构，其中 `resource` 设置为 `search`。提交的表达式在 `extra.search_query` 下返回。






{/* result-schema:start */}
## 标准结果字段

通过 `GET /v1/tasks/{task_id}?view=standard` 获取固定结果结构。MCP 查询使用 `view`/`fields`，Typed 工具使用 `_result_view`/`_result_fields`，CLI 使用 `--result-view`/`--result-fields`。声明字段在值为 `null` 时仍会保留；如需完整源数据，请使用任务结果文件。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 标准化的 id 字段。 |
| `platform` | `string` | 否 | 标准化的 platform 字段。 |
| `resource` | `string` | 否 | 标准化的 resource 字段。 |
| `type` | `string` | 否 | 标准化的 type 字段。 |
| `url` | `string` | 是 | 标准化的 url 字段。 |
| `published_at` | `string` | 是 | 标准化的 published at 字段。 |
| `collected_at` | `string` | 否 | 标准化的 collected at 字段。 |
| `text` | `string` | 是 | 标准化的 text 字段。 |
| `author` | `object` | 是 | 标准化的 author 字段。 |
| `author.id` | `string` | 是 | 标准化的 id 字段。 |
| `author.username` | `string` | 是 | 标准化的 username 字段。 |
| `author.name` | `string` | 是 | 标准化的 name 字段。 |
| `author.url` | `string` | 是 | 标准化的 url 字段。 |
| `author.avatar_url` | `string` | 是 | 标准化的 avatar url 字段。 |
| `author.is_verified` | `boolean` | 是 | 标准化的 is verified 字段。 |
| `author.is_private` | `boolean` | 是 | 标准化的 is private 字段。 |
| `metrics` | `object` | 是 | 标准化的 metrics 字段。 |
| `metrics.likes_count` | `integer` | 是 | 标准化的 likes count 字段。 |
| `metrics.comments_count` | `integer` | 是 | 标准化的 comments count 字段。 |
| `metrics.shares_count` | `integer` | 是 | 标准化的 shares count 字段。 |
| `metrics.views_count` | `integer` | 是 | 标准化的 views count 字段。 |
| `media` | `array<object>` | 是 | 标准化的 media 字段。 |
| `media.type` | `string` | 是 | 标准化的 type 字段。 |
| `media.url` | `string` | 是 | 标准化的 url 字段。 |
| `media.thumbnail_url` | `string` | 是 | 标准化的 thumbnail url 字段。 |
| `media.width` | `integer` | 是 | 标准化的 width 字段。 |
| `media.height` | `integer` | 是 | 标准化的 height 字段。 |
| `media.duration_seconds` | `number` | 是 | 标准化的 duration seconds 字段。 |
| `hashtags` | `array<string>` | 是 | 标准化的 hashtags 字段。 |
| `mentions` | `array<string>` | 是 | 标准化的 mentions 字段。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "WTKBMSYYT3XIZQH0",
    "status": "succeeded",
    "public_id": "x/search",
    "result_count": 20,
    "credits_amount": 14,
    "results": {
      "limit": 1,
      "next_cursor": "MjUyMg",
      "has_more": true,
      "view": "standard",
      "schema_id": "post",
      "schema_version": "1.0",
      "items": [
        {
          "id": "2077807981715128702",
          "platform": "x",
          "resource": "search",
          "type": "post",
          "url": "https://x.com/OpenAI/status/2077807981715128702",
          "published_at": "2026-07-16T17:30:01Z",
          "collected_at": "2026-07-17T07:05:45.186861Z",
          "text": "Listen to the OpenAI Podcast on—\n\nSpotify\nhttps://t.co/hLcRdGrfUX\nApple\nhttps://t.co/UsNCsk4dvq\nYouTube\nhttps://t.co/Ji9gUYXqYp",
          "author": {
            "id": "4398626122",
            "username": "OpenAI",
            "name": "OpenAI",
            "url": "https://x.com/OpenAI",
            "avatar_url": "https://pbs.twimg.com/profile_images/1885410181409820672/ztsaR0JW_normal.jpg",
            "is_verified": true,
            "is_private": null
          },
          "metrics": {
            "likes_count": 61,
            "comments_count": 7,
            "shares_count": null,
            "views_count": 49237
          },
          "media": [],
          "hashtags": [],
          "mentions": []
        }
      ]
    }
  }
}
```
