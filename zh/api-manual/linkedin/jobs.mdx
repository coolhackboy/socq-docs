---
title: "LinkedIn 职位"
description: "采集公开 LinkedIn 职位详细信息。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/linkedin/jobs"
---

# LinkedIn 职位

```json
{
  "urls": [
    "https://www.linkedin.com/jobs/view/4435421919/"
  ]
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/linkedin/jobs` |
| Compact MCP | `socq_execute` 与 `endpoint: "linkedin/jobs"` |
| Typed MCP | `socq_linkedin_jobs` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=linkedin_jobs` |
| CLI | `socq linkedin jobs` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}

每个 URL 必须标识一个职位详细信息页面。结果使用`resource="jobs"`和`type="job"`；职位、描述、公司、地点、就业类型和申请详细信息均已标准化。








{/* result-schema:start */}
## ????

????????????? SocQ ???????????????????????????????????????????????MCP ???? `fields`?Typed ???? `_result_fields`?CLI ?? `--result-fields`?????????? `null` ??????

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 当前结果项的唯一标识。 |
| `platform` | `string` | 否 | 结果所属的平台，例如 `instagram` 或 `linkedin`。 |
| `resource` | `string` | 否 | 该结果对应的 SocQ 资源，例如 `videos` 或 `comments`。 |
| `type` | `string` | 否 | 结果项的类别，例如 `video`、`post` 或 `comment`。 |
| `url` | `string` | 是 | 结果项的公开访问地址（如可获得）。 |
| `published_at` | `string` | 是 | 源内容的发布时间（如可获得），使用 ISO 8601 格式。 |
| `collected_at` | `string` | 否 | SocQ 采集或处理该结果的时间，使用 ISO 8601 格式。 |
| `title` | `string` | 是 | 职位名称。 |
| `company` | `object` | 是 | 职位所属公司。 |
| `company.id` | `string` | 是 | 公司的唯一标识。 |
| `company.name` | `string` | 是 | 公司的显示名称。 |
| `company.url` | `string` | 是 | 公司的公开访问地址（如可获得）。 |
| `company.logo_url` | `string` | 是 | 公司或广告主徽标的地址。 |
| `location` | `object` | 是 | 结果关联的地理位置信息。 |
| `location.name` | `string` | 是 | 便于阅读的地点名称。 |
| `location.city` | `string` | 是 | 城市名称。 |
| `location.region` | `string` | 是 | 地区、州或省份名称。 |
| `location.country` | `string` | 是 | 国家或地区名称或代码。 |
| `location.latitude` | `number` | 是 | 十进制度格式的纬度。 |
| `location.longitude` | `number` | 是 | 十进制度格式的经度。 |
| `text` | `string` | 是 | 职位描述。 |
| `employment_type` | `string` | 是 | 雇佣类型，例如全职、兼职或合同制。 |
| `experience_level` | `string` | 是 | 职位要求的经验或职级。 |
| `is_remote` | `boolean` | 是 | 职位是否标记为远程工作。 |
| `salary` | `object` | 是 | 职位标注的薪酬信息。 |
| `salary.amount` | `number` | 是 | 货币金额的数值。 |
| `salary.currency` | `string` | 是 | 货币金额所使用的币种代码或符号。 |
| `salary.formatted` | `string` | 是 | 便于阅读的价格或薪酬文本。 |
| `expires_at` | `string` | 是 | 职位或条目的到期时间（如可获得），使用 ISO 8601 格式。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "linkedin/jobs",
    "result_count": 1,
    "credits_amount": 0.8,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "schema_id": "job",
      "schema_version": "1.0",
      "items": [
        {
          "id": "3986111804",
          "platform": "linkedin",
          "resource": "jobs",
          "type": "job",
          "url": "https://www.linkedin.com/jobs/view/3986111804",
          "published_at": "2026-07-15T08:00:00Z",
          "collected_at": "2026-07-17T10:30:00Z",
          "title": "Software Engineer",
          "company": null,
          "location": null,
          "text": "Build and maintain customer-facing software.",
          "employment_type": null,
          "experience_level": null,
          "is_remote": null,
          "salary": null,
          "expires_at": null
        }
      ]
    }
  }
}
```
