---
title: "LinkedIn 职位"
description: "采集公开 LinkedIn 职位详细信息。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/linkedin/jobs"
---

# LinkedIn 职位

```json
{
  "urls": [
    "https://www.linkedin.com/jobs/view/4435421919/"
  ]
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/linkedin/jobs` |
| Compact MCP | `socq_execute` 与 `endpoint: "linkedin/jobs"` |
| Typed MCP | `socq_linkedin_jobs` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=linkedin_jobs` |
| CLI | `socq linkedin jobs` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}

每个 URL 必须标识一个职位详细信息页面。结果使用`resource="jobs"`和`type="job"`；职位、描述、公司、地点、就业类型和申请详细信息均已标准化。






{/* result-schema:start */}
## 标准结果字段

通过 `GET /v1/tasks/{task_id}?view=standard` 获取固定结果结构。MCP 查询使用 `view`/`fields`，Typed 工具使用 `_result_view`/`_result_fields`，CLI 使用 `--result-view`/`--result-fields`。声明字段在值为 `null` 时仍会保留；如需完整源数据，请使用任务结果文件。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 标准化的 id 字段。 |
| `platform` | `string` | 否 | 标准化的 platform 字段。 |
| `resource` | `string` | 否 | 标准化的 resource 字段。 |
| `type` | `string` | 否 | 标准化的 type 字段。 |
| `url` | `string` | 是 | 标准化的 url 字段。 |
| `published_at` | `string` | 是 | 标准化的 published at 字段。 |
| `collected_at` | `string` | 否 | 标准化的 collected at 字段。 |
| `title` | `string` | 是 | 标准化的 title 字段。 |
| `company` | `object` | 是 | 标准化的 company 字段。 |
| `company.id` | `string` | 是 | 标准化的 id 字段。 |
| `company.name` | `string` | 是 | 标准化的 name 字段。 |
| `company.url` | `string` | 是 | 标准化的 url 字段。 |
| `company.logo_url` | `string` | 是 | 标准化的 logo url 字段。 |
| `location` | `object` | 是 | 标准化的 location 字段。 |
| `location.name` | `string` | 是 | 标准化的 name 字段。 |
| `location.city` | `string` | 是 | 标准化的 city 字段。 |
| `location.region` | `string` | 是 | 标准化的 region 字段。 |
| `location.country` | `string` | 是 | 标准化的 country 字段。 |
| `location.latitude` | `number` | 是 | 标准化的 latitude 字段。 |
| `location.longitude` | `number` | 是 | 标准化的 longitude 字段。 |
| `text` | `string` | 是 | 标准化的 text 字段。 |
| `employment_type` | `string` | 是 | 标准化的 employment type 字段。 |
| `experience_level` | `string` | 是 | 标准化的 experience level 字段。 |
| `is_remote` | `boolean` | 是 | 标准化的 is remote 字段。 |
| `salary` | `object` | 是 | 标准化的 salary 字段。 |
| `salary.amount` | `number` | 是 | 标准化的 amount 字段。 |
| `salary.currency` | `string` | 是 | 标准化的 currency 字段。 |
| `salary.formatted` | `string` | 是 | 标准化的 formatted 字段。 |
| `expires_at` | `string` | 是 | 标准化的 expires at 字段。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "linkedin/jobs",
    "result_count": 1,
    "credits_amount": 0.8,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "view": "standard",
      "schema_id": "job",
      "schema_version": "1.0",
      "items": [
        {
          "id": "3986111804",
          "platform": "linkedin",
          "resource": "jobs",
          "type": "job",
          "url": "https://www.linkedin.com/jobs/view/3986111804",
          "published_at": "2026-07-15T08:00:00Z",
          "collected_at": "2026-07-17T10:30:00Z",
          "title": "Software Engineer",
          "company": null,
          "location": null,
          "text": "Build and maintain customer-facing software.",
          "employment_type": null,
          "experience_level": null,
          "is_remote": null,
          "salary": null,
          "expires_at": null
        }
      ]
    }
  }
}
```
