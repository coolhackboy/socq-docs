---
title: "LinkedIn 帖子评论"
description: "采集并标准化公开的 LinkedIn 帖子评论。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/linkedin/post-comments"
---

# LinkedIn 帖子评论

```json
{
  "url": "https://www.linkedin.com/posts/aagupta_what-you-need-to-know-ai-agents-activity-7354600338621906944-RvXR"
}
```

接口返回帖子公开提供的全部评论，并按成功存储的实际评论条数计费。








{/* result-schema:start */}
## 结果字段

任务结果使用下列稳定的 SocQ 字段。来源特有的字段名称和格式会被规范化，但不会推断或补充来源未提供的值。如需请求更小的字段集，MCP 查询请使用 `fields`，Typed 工具请使用 `_result_fields`，CLI 请使用 `--result-fields`。即使值为 `null`，已声明的字段仍会保留。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 当前结果项的唯一标识。 |
| `platform` | `string` | 否 | 结果所属的平台，例如 `instagram` 或 `linkedin`。 |
| `resource` | `string` | 否 | 该结果对应的 SocQ 资源，例如 `videos` 或 `comments`。 |
| `type` | `string` | 否 | 结果项的类别，例如 `video`、`post` 或 `comment`。 |
| `url` | `string` | 是 | 结果项的公开访问地址（如可获得）。 |
| `published_at` | `string` | 是 | 源内容的发布时间（如可获得），使用 ISO 8601 格式。 |
| `collected_at` | `string` | 否 | SocQ 采集或处理该结果的时间，使用 ISO 8601 格式。 |
| `text` | `string` | 是 | 评论或回复的文本内容。 |
| `author` | `object` | 是 | 内容的作者或发布者。 |
| `author.id` | `string` | 是 | 作者的唯一标识。 |
| `author.username` | `string` | 是 | 账号的公开用户名或账号标识。 |
| `author.name` | `string` | 是 | 作者的显示名称。 |
| `author.url` | `string` | 是 | 作者的公开访问地址（如可获得）。 |
| `author.avatar_url` | `string` | 是 | 账号头像图片的地址。 |
| `author.is_verified` | `boolean` | 是 | 账号是否已通过平台认证。 |
| `author.is_private` | `boolean` | 是 | 账号是否为私密账号。 |
| `parent_id` | `string` | 是 | 回复所对应的父评论标识。 |
| `post_id` | `string` | 是 | 评论所属帖子的标识。 |
| `metrics` | `object` | 是 | 结果项可获得的计数和度量数据。 |
| `metrics.likes_count` | `integer` | 是 | 点赞数量。 |
| `metrics.replies_count` | `integer` | 是 | 回复数量。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "MUH5DCQXDCZ3ZAGW",
    "status": "succeeded",
    "public_id": "linkedin/post-comments",
    "result_count": 1,
    "credits_amount": 0.5,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "schema_id": "comment",
      "schema_version": "1.0",
      "items": [
        {
          "id": null,
          "platform": "linkedin",
          "resource": "post-comments",
          "type": "comment",
          "url": "https://www.linkedin.com/posts/aagupta_what-you-need-to-know-ai-agents-activity-7354600338621906944-RvXR",
          "published_at": null,
          "collected_at": "2026-08-11T03:03:36.428610Z",
          "text": "This hit different tbh bruh",
          "author": {
            "id": null,
            "username": null,
            "name": "Akil Rakuten A.",
            "url": "https://www.linkedin.com/in/akil-rakuten-a-60739a229",
            "avatar_url": null,
            "is_verified": null,
            "is_private": null
          },
          "parent_id": null,
          "post_id": null,
          "metrics": null
        }
      ]
    }
  }
}
```

## 与 MCP 和 CLI 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/linkedin/post-comments` |
| Compact MCP | `socq_execute` 与 `endpoint: "linkedin/post-comments"` |
| Typed MCP | `socq_linkedin_post_comments` |
| CLI | `socq linkedin post-comments` |

该接口使用异步任务流程并返回 `task_id`。轮询任务接口以读取标准化结果。
