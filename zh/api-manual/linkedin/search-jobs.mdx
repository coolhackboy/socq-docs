---
title: "LinkedIn 职位搜索"
description: "搜索并标准化公开的 LinkedIn 职位数据。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/linkedin/search-jobs"
---

# LinkedIn 职位搜索

```json
{
  "location": "Seattle",
  "keyword": "Python",
  "country": "US",
  "results_limit": 20
}
```

## 与 MCP 和 CLI 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/linkedin/search-jobs` |
| Compact MCP | `socq_execute` 与 `endpoint: "linkedin/search-jobs"` |
| Typed MCP | `socq_linkedin_search_jobs` |
| CLI | `socq linkedin search-jobs` |

该接口使用异步任务流程并返回 `task_id`。轮询任务接口以读取标准化结果。
