---
title: "Instagram 关注列表"
description: "采集 Instagram 账户后面的公共个人资料。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/instagram/following-list"
---

# Instagram 关注列表

```json
{"usernames": ["instagram"], "results_limit": 100}
```

## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/instagram/following-list` |
| Compact MCP | `socq_execute` 与 `endpoint: "instagram/following-list"` |
| Typed MCP | `socq_instagram_following` |
| CLI | `socq instagram following` |

只能返回对公共个人资料可见的关系。






{/* result-schema:start */}
## 标准结果字段

通过 `GET /v1/tasks/{task_id}?view=standard` 获取固定结果结构。MCP 查询使用 `view`/`fields`，Typed 工具使用 `_result_view`/`_result_fields`，CLI 使用 `--result-view`/`--result-fields`。声明字段在值为 `null` 时仍会保留；如需完整源数据，请使用任务结果文件。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 标准化的 id 字段。 |
| `platform` | `string` | 否 | 标准化的 platform 字段。 |
| `resource` | `string` | 否 | 标准化的 resource 字段。 |
| `type` | `string` | 否 | 标准化的 type 字段。 |
| `url` | `string` | 是 | 标准化的 url 字段。 |
| `published_at` | `string` | 是 | 标准化的 published at 字段。 |
| `collected_at` | `string` | 否 | 标准化的 collected at 字段。 |
| `actor` | `object` | 是 | 标准化的 actor 字段。 |
| `actor.id` | `string` | 是 | 标准化的 id 字段。 |
| `actor.username` | `string` | 是 | 标准化的 username 字段。 |
| `actor.name` | `string` | 是 | 标准化的 name 字段。 |
| `actor.url` | `string` | 是 | 标准化的 url 字段。 |
| `actor.avatar_url` | `string` | 是 | 标准化的 avatar url 字段。 |
| `actor.is_verified` | `boolean` | 是 | 标准化的 is verified 字段。 |
| `actor.is_private` | `boolean` | 是 | 标准化的 is private 字段。 |
| `related_actor` | `object` | 是 | 标准化的 related actor 字段。 |
| `related_actor.id` | `string` | 是 | 标准化的 id 字段。 |
| `related_actor.username` | `string` | 是 | 标准化的 username 字段。 |
| `related_actor.name` | `string` | 是 | 标准化的 name 字段。 |
| `related_actor.url` | `string` | 是 | 标准化的 url 字段。 |
| `related_actor.avatar_url` | `string` | 是 | 标准化的 avatar url 字段。 |
| `related_actor.is_verified` | `boolean` | 是 | 标准化的 is verified 字段。 |
| `related_actor.is_private` | `boolean` | 是 | 标准化的 is private 字段。 |
| `relationship` | `string` | 是 | 标准化的 relationship 字段。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "WKNIUSMO2POSO9Y7",
    "status": "succeeded",
    "public_id": "instagram/following-list",
    "result_count": 5,
    "credits_amount": 1.5,
    "results": {
      "limit": 1,
      "next_cursor": "NjQ3MQ",
      "has_more": true,
      "view": "standard",
      "schema_id": "relationship",
      "schema_version": "1.0",
      "items": [
        {
          "id": "5516590483",
          "platform": "instagram",
          "resource": "following-list",
          "type": "profile",
          "url": "https://www.instagram.com/prodlaila/",
          "published_at": null,
          "collected_at": "2026-07-31T02:22:43.919697Z",
          "actor": null,
          "related_actor": {
            "id": "5516590483",
            "username": null,
            "name": null,
            "url": "https://www.instagram.com/prodlaila/",
            "avatar_url": null,
            "is_verified": null,
            "is_private": null
          },
          "relationship": "following"
        }
      ]
    }
  }
}
```
