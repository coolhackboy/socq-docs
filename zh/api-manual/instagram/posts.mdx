---
title: "Instagram 帖子"
description: "采集公开 Instagram 发布数据。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/instagram/posts"
---

# Instagram 帖子

采集公共 Instagram 帖子数据，包括标题文本、参与度指标、媒体 URL、作者字段和原始 JSONL 输出。

## 端点

```http
POST https://api.socq.ai/v1/instagram/posts
```

## 请求正文

```json
{
  "usernames": ["instagram"],
  "results_limit": 10
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/instagram/posts` |
| Compact MCP | `socq_execute` 与 `endpoint: "instagram/posts"` |
| Typed MCP | `socq_instagram_posts` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=instagram_posts` |
| CLI | `socq instagram posts` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}








{/* result-schema:start */}
## ????

????????????? SocQ ???????????????????????????????????????????????MCP ???? `fields`?Typed ???? `_result_fields`?CLI ?? `--result-fields`?????????? `null` ??????

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 当前结果项的唯一标识。 |
| `platform` | `string` | 否 | 结果所属的平台，例如 `instagram` 或 `linkedin`。 |
| `resource` | `string` | 否 | 该结果对应的 SocQ 资源，例如 `videos` 或 `comments`。 |
| `type` | `string` | 否 | 结果项的类别，例如 `video`、`post` 或 `comment`。 |
| `url` | `string` | 是 | 结果项的公开访问地址（如可获得）。 |
| `published_at` | `string` | 是 | 源内容的发布时间（如可获得），使用 ISO 8601 格式。 |
| `collected_at` | `string` | 否 | SocQ 采集或处理该结果的时间，使用 ISO 8601 格式。 |
| `text` | `string` | 是 | 帖子的正文、说明文字或消息内容。 |
| `author` | `object` | 是 | 内容的作者或发布者。 |
| `author.id` | `string` | 是 | 作者的唯一标识。 |
| `author.username` | `string` | 是 | 账号的公开用户名或账号标识。 |
| `author.name` | `string` | 是 | 作者的显示名称。 |
| `author.url` | `string` | 是 | 作者的公开访问地址（如可获得）。 |
| `author.avatar_url` | `string` | 是 | 账号头像图片的地址。 |
| `author.is_verified` | `boolean` | 是 | 账号是否已通过平台认证。 |
| `author.is_private` | `boolean` | 是 | 账号是否为私密账号。 |
| `metrics` | `object` | 是 | 结果项可获得的计数和度量数据。 |
| `metrics.likes_count` | `integer` | 是 | 点赞数量。 |
| `metrics.comments_count` | `integer` | 是 | 评论数量。 |
| `metrics.shares_count` | `integer` | 是 | 分享或转发数量。 |
| `metrics.views_count` | `integer` | 是 | 平台报告的观看数量。 |
| `media` | `array<object>` | 是 | 结果附带的图片、视频或其他媒体。 |
| `media.type` | `string` | 是 | 媒体类型，例如 `image` 或 `video`。 |
| `media.url` | `string` | 是 | 媒体文件的地址。 |
| `media.thumbnail_url` | `string` | 是 | 媒体预览图的地址。 |
| `media.width` | `integer` | 是 | 媒体宽度，单位为像素。 |
| `media.height` | `integer` | 是 | 媒体高度，单位为像素。 |
| `media.duration_seconds` | `number` | 是 | 媒体时长，单位为秒。 |
| `hashtags` | `array<string>` | 是 | 内容关联的话题标签，保留源数据中的拼写。 |
| `mentions` | `array<string>` | 是 | 内容中提及的账号。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "instagram/posts",
    "result_count": 1,
    "credits_amount": 0.34,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "schema_id": "post",
      "schema_version": "1.0",
      "items": [
        {
          "id": "3936101605334124689",
          "platform": "instagram",
          "resource": "posts",
          "type": "post",
          "url": "https://www.instagram.com/p/Daf145ByQiR/",
          "published_at": "2026-07-07T16:00:36Z",
          "collected_at": "2026-07-08T08:59:45Z",
          "text": "Post caption text",
          "author": {
            "id": "25025320",
            "username": "instagram",
            "name": "Instagram",
            "url": "https://www.instagram.com/instagram",
            "avatar_url": null,
            "is_verified": null,
            "is_private": null
          },
          "metrics": {
            "likes_count": 109798,
            "comments_count": 4731,
            "shares_count": null,
            "views_count": 5780995
          },
          "media": [
            {
              "type": "video",
              "url": "https://example.com/video.mp4",
              "thumbnail_url": "https://example.com/thumbnail.jpg",
              "width": 1080,
              "height": 1920,
              "duration_seconds": 29.9
            }
          ],
          "hashtags": [],
          "mentions": [
            "creator"
          ]
        }
      ]
    }
  }
}
```

您可以在同一字段中传递多个公共用户名：

```json
{
  "usernames": [
    "instagram",
    "creators"
  ],
  "results_limit": 10
}
```
