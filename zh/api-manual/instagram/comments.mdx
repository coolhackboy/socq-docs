---
title: "Instagram 评论"
description: "采集公众Instagram评论数据。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/instagram/comments"
---

# Instagram 评论

采集公共帖子 URL 的公共 Instagram 评论数据，包括评论文本、作者字段、参与度指标和原始 JSONL 输出。

## 端点

```http
POST https://api.socq.ai/v1/instagram/comments
```

## 请求正文

```json
{
  "urls": [
    "https://www.instagram.com/reel/DBZMUClOJjZ/"
  ]
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/instagram/comments` |
| Compact MCP | `socq_execute` 与 `endpoint: "instagram/comments"` |
| Typed MCP | `socq_instagram_comments` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=instagram_comments` |
| CLI | `socq instagram comments` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}

所有返回评论均按实际结果数计费。此端点不接受结果计数限制。






{/* result-schema:start */}
## 标准结果字段

通过 `GET /v1/tasks/{task_id}?view=standard` 获取固定结果结构。MCP 查询使用 `view`/`fields`，Typed 工具使用 `_result_view`/`_result_fields`，CLI 使用 `--result-view`/`--result-fields`。声明字段在值为 `null` 时仍会保留；如需完整源数据，请使用任务结果文件。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 标准化的 id 字段。 |
| `platform` | `string` | 否 | 标准化的 platform 字段。 |
| `resource` | `string` | 否 | 标准化的 resource 字段。 |
| `type` | `string` | 否 | 标准化的 type 字段。 |
| `url` | `string` | 是 | 标准化的 url 字段。 |
| `published_at` | `string` | 是 | 标准化的 published at 字段。 |
| `collected_at` | `string` | 否 | 标准化的 collected at 字段。 |
| `text` | `string` | 是 | 标准化的 text 字段。 |
| `author` | `object` | 是 | 标准化的 author 字段。 |
| `author.id` | `string` | 是 | 标准化的 id 字段。 |
| `author.username` | `string` | 是 | 标准化的 username 字段。 |
| `author.name` | `string` | 是 | 标准化的 name 字段。 |
| `author.url` | `string` | 是 | 标准化的 url 字段。 |
| `author.avatar_url` | `string` | 是 | 标准化的 avatar url 字段。 |
| `author.is_verified` | `boolean` | 是 | 标准化的 is verified 字段。 |
| `author.is_private` | `boolean` | 是 | 标准化的 is private 字段。 |
| `parent_id` | `string` | 是 | 标准化的 parent id 字段。 |
| `post_id` | `string` | 是 | 标准化的 post id 字段。 |
| `metrics` | `object` | 是 | 标准化的 metrics 字段。 |
| `metrics.likes_count` | `integer` | 是 | 标准化的 likes count 字段。 |
| `metrics.replies_count` | `integer` | 是 | 标准化的 replies count 字段。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "instagram/comments",
    "result_count": 1,
    "credits_amount": 0.3,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "view": "standard",
      "schema_id": "comment",
      "schema_version": "1.0",
      "items": [
        {
          "id": "18000000000000000",
          "platform": "instagram",
          "resource": "comments",
          "type": "comment",
          "url": "https://www.instagram.com/p/POST_CODE/",
          "published_at": "2026-07-07T16:00:36Z",
          "collected_at": "2026-07-08T08:59:45Z",
          "text": "Comment text",
          "author": {
            "id": "25025320",
            "username": "instagram",
            "name": "Instagram",
            "url": "https://www.instagram.com/instagram",
            "avatar_url": null,
            "is_verified": null,
            "is_private": null
          },
          "parent_id": null,
          "post_id": null,
          "metrics": {
            "likes_count": 24,
            "replies_count": 3
          }
        }
      ]
    }
  }
}
```
