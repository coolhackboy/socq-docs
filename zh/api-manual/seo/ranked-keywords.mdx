---
title: "Ranked Keywords"
description: "Find keywords a domain, subdomain, or page already ranks for."
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/seo/ranked-keywords"
---

# Ranked Keywords

```json
{
  "target": "microsoft.com",
  "location_code": 2840,
  "language_code": "en",
  "results_limit": 5
}
```

Each item is one ranked keyword record with keyword metrics and the matching search-result position.






{/* result-schema:start */}
## 标准结果字段

通过 `GET /v1/tasks/{task_id}?view=standard` 获取固定结果结构。MCP 查询使用 `view`/`fields`，Typed 工具使用 `_result_view`/`_result_fields`，CLI 使用 `--result-view`/`--result-fields`。声明字段在值为 `null` 时仍会保留；如需完整源数据，请使用任务结果文件。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 标准化的 id 字段。 |
| `platform` | `string` | 否 | 标准化的 platform 字段。 |
| `resource` | `string` | 否 | 标准化的 resource 字段。 |
| `type` | `string` | 否 | 标准化的 type 字段。 |
| `url` | `string` | 是 | 标准化的 url 字段。 |
| `published_at` | `string` | 是 | 标准化的 published at 字段。 |
| `collected_at` | `string` | 否 | 标准化的 collected at 字段。 |
| `keyword` | `string` | 是 | 标准化的 keyword 字段。 |
| `search_volume` | `integer` | 是 | 标准化的 search volume 字段。 |
| `difficulty` | `number` | 是 | 标准化的 difficulty 字段。 |
| `intent` | `string` | 是 | 标准化的 intent 字段。 |
| `cpc` | `number` | 是 | 标准化的 cpc 字段。 |
| `competition` | `number` | 是 | 标准化的 competition 字段。 |
| `rank` | `integer` | 是 | 标准化的 rank 字段。 |
| `target_url` | `string` | 是 | 标准化的 target url 字段。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "V42P9U42V10HYI44",
    "status": "succeeded",
    "public_id": "seo/ranked-keywords",
    "result_count": 5,
    "credits_amount": 0.5,
    "results": {
      "limit": 1,
      "next_cursor": "OTc3OA",
      "has_more": true,
      "view": "standard",
      "schema_id": "seo-keyword",
      "schema_version": "1.0",
      "items": [
        {
          "id": "0 0 excel",
          "platform": "seo",
          "resource": "ranked-keywords",
          "type": "ranked-keywords",
          "url": "https://support.microsoft.com/en-us/office/display-or-hide-zero-values-3ec7a433-46b8-4516-8085-a00e9e476b03",
          "published_at": null,
          "collected_at": "2026-07-31T02:44:32.835914Z",
          "keyword": "0 0 excel",
          "search_volume": null,
          "difficulty": null,
          "intent": null,
          "cpc": null,
          "competition": null,
          "rank": null,
          "target_url": null
        }
      ]
    }
  }
}
```
