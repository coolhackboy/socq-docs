---
title: "Google Organic SERP"
description: "Retrieve live Google organic search results."
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/seo/google-organic-serp"
---

# Google Organic SERP

```json
{"query":"keyword research","location_code":2840,"language_code":"en","device":"desktop","results_limit":10}
```

Each item is one search-result element with its URL, title, rank, and available enriched fields.






{/* result-schema:start */}
## 标准结果字段

通过 `GET /v1/tasks/{task_id}?view=standard` 获取固定结果结构。MCP 查询使用 `view`/`fields`，Typed 工具使用 `_result_view`/`_result_fields`，CLI 使用 `--result-view`/`--result-fields`。声明字段在值为 `null` 时仍会保留；如需完整源数据，请使用任务结果文件。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 标准化的 id 字段。 |
| `platform` | `string` | 否 | 标准化的 platform 字段。 |
| `resource` | `string` | 否 | 标准化的 resource 字段。 |
| `type` | `string` | 否 | 标准化的 type 字段。 |
| `url` | `string` | 是 | 标准化的 url 字段。 |
| `published_at` | `string` | 是 | 标准化的 published at 字段。 |
| `collected_at` | `string` | 否 | 标准化的 collected at 字段。 |
| `title` | `string` | 是 | 标准化的 title 字段。 |
| `text` | `string` | 是 | 标准化的 text 字段。 |
| `rank` | `integer` | 是 | 标准化的 rank 字段。 |
| `domain` | `string` | 是 | 标准化的 domain 字段。 |
| `result_type` | `string` | 是 | 标准化的 result type 字段。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "Z6OEM0TKEEZFLLJ8",
    "status": "succeeded",
    "public_id": "seo/google-organic-serp",
    "result_count": 8,
    "credits_amount": 0.8,
    "results": {
      "limit": 1,
      "next_cursor": "NjU4Nw",
      "has_more": true,
      "view": "standard",
      "schema_id": "serp-result",
      "schema_version": "1.0",
      "items": [
        {
          "id": "https://www.semrush.com/analytics/keywordmagic/",
          "platform": "seo",
          "resource": "google-organic-serp",
          "type": "google-organic-serp",
          "url": "https://www.semrush.com/analytics/keywordmagic/",
          "published_at": null,
          "collected_at": "2026-07-31T02:25:00.673956Z",
          "title": "Free Keyword Tool: Find the Right Keywords for SEO & AI ...",
          "text": "Free Keyword Tool: Find the Right Keywords for SEO & AI ...",
          "rank": null,
          "domain": null,
          "result_type": "google-organic-serp"
        }
      ]
    }
  }
}
```
