---
title: "Keyword Suggestions"
description: "Find long-tail keywords containing a seed phrase."
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/seo/keyword-suggestions"
---

# Keyword Suggestions

```json
{"query":"keyword research","location_code":2840,"language_code":"en","results_limit":100}
```

Each item in `results.items` is one returned keyword record. `results_limit` can be 1鈥?,000.






{/* result-schema:start */}
## 标准结果字段

通过 `GET /v1/tasks/{task_id}?view=standard` 获取固定结果结构。MCP 查询使用 `view`/`fields`，Typed 工具使用 `_result_view`/`_result_fields`，CLI 使用 `--result-view`/`--result-fields`。声明字段在值为 `null` 时仍会保留；如需完整源数据，请使用任务结果文件。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 标准化的 id 字段。 |
| `platform` | `string` | 否 | 标准化的 platform 字段。 |
| `resource` | `string` | 否 | 标准化的 resource 字段。 |
| `type` | `string` | 否 | 标准化的 type 字段。 |
| `url` | `string` | 是 | 标准化的 url 字段。 |
| `published_at` | `string` | 是 | 标准化的 published at 字段。 |
| `collected_at` | `string` | 否 | 标准化的 collected at 字段。 |
| `keyword` | `string` | 是 | 标准化的 keyword 字段。 |
| `search_volume` | `integer` | 是 | 标准化的 search volume 字段。 |
| `difficulty` | `number` | 是 | 标准化的 difficulty 字段。 |
| `intent` | `string` | 是 | 标准化的 intent 字段。 |
| `cpc` | `number` | 是 | 标准化的 cpc 字段。 |
| `competition` | `number` | 是 | 标准化的 competition 字段。 |
| `rank` | `integer` | 是 | 标准化的 rank 字段。 |
| `target_url` | `string` | 是 | 标准化的 target url 字段。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "RWZ4UEU3P31E08SK",
    "status": "succeeded",
    "public_id": "seo/keyword-suggestions",
    "result_count": 5,
    "credits_amount": 0.5,
    "results": {
      "limit": 1,
      "next_cursor": "NjU5Nw",
      "has_more": true,
      "view": "standard",
      "schema_id": "seo-keyword",
      "schema_version": "1.0",
      "items": [
        {
          "id": "keyword research",
          "platform": "seo",
          "resource": "keyword-suggestions",
          "type": "keyword-suggestions",
          "url": null,
          "published_at": null,
          "collected_at": "2026-07-31T02:25:18.257457Z",
          "keyword": "keyword research",
          "search_volume": null,
          "difficulty": null,
          "intent": "{'se_type': 'google', 'main_intent': 'informational', 'foreign_intent': None, 'last_updated_time': '2026-04-28 07:15:35 +00:00'}",
          "cpc": null,
          "competition": null,
          "rank": null,
          "target_url": null
        }
      ]
    }
  }
}
```
