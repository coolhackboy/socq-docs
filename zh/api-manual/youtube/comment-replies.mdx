---
title: "YouTube 评论回复"
description: "采集公开 YouTube 评论的回复。"
openapi: "/zh/api-manual/agent-api/agent-api.json POST /v1/youtube/comment-replies"
---

# YouTube 评论回复

采集公开 YouTube 评论的回复。结果会转换为统一结构，并按实际保存的记录数计费。

```json
{
  "url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ",
  "comment_id": "Ugzge340dBgB75hWBm54AaABAg",
  "results_limit": 20
}
```

<Info>
  请传入稳定的视频 URL 和父评论 ID，SOCQ 会在服务端自动解析当前 continuation token。
  旧版 `continuation_token` 入参仍然兼容，但不应保存为可复用示例。
</Info>

## 回复解析错误

| 错误码 | 含义 |
| --- | --- |
| `youtube_parent_comment_not_found` | 在指定视频 URL 中未找到父评论。 |
| `youtube_reply_token_missing` | 父评论未提供回复 continuation token。 |

{/* mcp-usage:start */}
## 通过 MCP 使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/youtube/comment-replies` |
| Compact MCP | `socq_execute`，并设置 `endpoint: "youtube/comment-replies"` |
| 类型化 MCP | `socq_youtube_comment_replies` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=youtube_comment_replies` |
| CLI | `socq youtube comment-replies` |

使用 `socq_execute` 时，将上面的 JSON 请求体作为 `input` 对象传入；也可以将相同字段直接传给类型化 MCP 工具。类型化 MCP 工具还支持 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  提交成功后会返回 `task_id`。继续调用 `socq_get_task` 直到任务成功，然后从 `results.items` 读取标准化记录。
</Info>

完整的提交和轮询流程请参阅 [MCP 快速开始](/zh/integrations/mcp)。
{/* mcp-usage:end */}






{/* result-schema:start */}
## 标准结果字段

通过 `GET /v1/tasks/{task_id}?view=standard` 获取固定结果结构。MCP 查询使用 `view`/`fields`，Typed 工具使用 `_result_view`/`_result_fields`，CLI 使用 `--result-view`/`--result-fields`。声明字段在值为 `null` 时仍会保留；如需完整源数据，请使用任务结果文件。

| 字段 | 类型 | 可空 | 说明 |
| --- | --- | --- | --- |
| `id` | `string` | 是 | 标准化的 id 字段。 |
| `platform` | `string` | 否 | 标准化的 platform 字段。 |
| `resource` | `string` | 否 | 标准化的 resource 字段。 |
| `type` | `string` | 否 | 标准化的 type 字段。 |
| `url` | `string` | 是 | 标准化的 url 字段。 |
| `published_at` | `string` | 是 | 标准化的 published at 字段。 |
| `collected_at` | `string` | 否 | 标准化的 collected at 字段。 |
| `text` | `string` | 是 | 标准化的 text 字段。 |
| `author` | `object` | 是 | 标准化的 author 字段。 |
| `author.id` | `string` | 是 | 标准化的 id 字段。 |
| `author.username` | `string` | 是 | 标准化的 username 字段。 |
| `author.name` | `string` | 是 | 标准化的 name 字段。 |
| `author.url` | `string` | 是 | 标准化的 url 字段。 |
| `author.avatar_url` | `string` | 是 | 标准化的 avatar url 字段。 |
| `author.is_verified` | `boolean` | 是 | 标准化的 is verified 字段。 |
| `author.is_private` | `boolean` | 是 | 标准化的 is private 字段。 |
| `parent_id` | `string` | 是 | 标准化的 parent id 字段。 |
| `post_id` | `string` | 是 | 标准化的 post id 字段。 |
| `metrics` | `object` | 是 | 标准化的 metrics 字段。 |
| `metrics.likes_count` | `integer` | 是 | 标准化的 likes count 字段。 |
| `metrics.replies_count` | `integer` | 是 | 标准化的 replies count 字段。 |
{/* result-schema:end */}

## 任务完成响应

```json
{
  "code": 200,
  "data": {
    "task_id": "A1B2C3D4E5F6G7H8",
    "status": "succeeded",
    "public_id": "youtube/comment-replies",
    "result_count": 1,
    "credits_amount": 0.5,
    "results": {
      "limit": 50,
      "next_cursor": null,
      "has_more": false,
      "view": "standard",
      "schema_id": "comment",
      "schema_version": "1.0",
      "items": [
        {
          "id": "example-id",
          "platform": "youtube",
          "resource": "comment-replies",
          "type": "comment",
          "url": "https://www.youtube.com/example/item",
          "published_at": "2026-07-24T08:00:00Z",
          "collected_at": "2026-07-24T10:30:00Z",
          "text": "Example public comment content.",
          "author": {
            "id": "author-id",
            "username": "example",
            "name": "Example",
            "url": "https://www.youtube.com/example",
            "avatar_url": null,
            "is_verified": null,
            "is_private": null
          },
          "parent_id": null,
          "post_id": null,
          "metrics": null
        }
      ]
    }
  }
}
```
