---
title: "YouTube 视频"
description: "采集公共 YouTube 视频元数据。"
openapi: "/api-manual/agent-api/agent-api.json POST /v1/youtube/videos"
---

# YouTube 视频

采集一个或多个公共视频 URL 的元数据。

```json
{
  "urls": ["https://www.youtube.com/watch?v=VIDEO_ID"]
}
```

{/* mcp-usage:start */}
## 与 MCP 一起使用

| 接口 | 值 |
| --- | --- |
| REST | `POST /v1/youtube/videos` |
| Compact MCP | `socq_execute` 与 `endpoint: "youtube/videos"` |
| Typed MCP | `socq_youtube_videos` |
| 精确工具 URL | `https://api.socq.ai/mcp?tools=youtube_videos` |
| CLI | `socq youtube videos` |

使用上面的 JSON 请求正文作为 `socq_execute` 的嵌套 `input` 对象，或将相同的字段直接传递给Typed 工具。Typed 工具还接受 `_wait_seconds`、`_result_limit` 和 `_idempotency_key`。

<Info>
  成功的采集将返回 `task_id`。继续 `socq_get_task` 直到任务成功，然后从 `results.items` 读取规范化记录。
</Info>

有关完整的提交和轮询流程，请参阅[MCP 快速入门](/zh/integrations/mcp)。
{/* mcp-usage:end */}

## 结果项

```json
{
  "id": "VIDEO_ID",
  "platform": "youtube",
  "resource": "videos",
  "type": "video",
  "title": "Example video",
  "url": "https://www.youtube.com/watch?v=VIDEO_ID",
  "description": "Video description",
  "author": {
    "id": "UC123456789",
    "username": "example",
    "name": "Example Channel",
    "url": "https://www.youtube.com/@example"
  },
  "metrics": {
    "views_count": 100000,
    "likes_count": 5000,
    "comments_count": 320
  },
  "duration_seconds": 327,
  "created_at": "2026-07-01T08:00:00",
  "collected_at": "2026-07-15T10:30:00",
  "extra": {}
}
```
